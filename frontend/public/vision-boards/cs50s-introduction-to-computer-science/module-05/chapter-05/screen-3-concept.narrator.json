{
  "module": 5,
  "chapter": 5,
  "screen": 3,
  "type": "concept",
  "createdAt": "2026-08-12T10:45:40.138Z",
  "narrationScript": "Now that we have a connection, let's perform some basic CRUD operations. CRUD stands for Create, Read, Update, and Delete, which are the fundamental interactions you'll have with any database. To Create new data, you'll use an `INSERT` statement. To Read data, you'll use `SELECT`, often with `fetchone()` to get a single row or `fetchall()` for multiple rows. `UPDATE` statements modify existing data, and `DELETE` statements remove data. When inserting or updating data based on user input, it's a **critical security best practice** to use *parameterized queries*, also known as prepared statements. Instead of directly embedding user-provided values into your SQL string, you use placeholders, like the question mark `?` for `sqlite3`, and pass the values as a separate tuple. This prevents a notorious vulnerability called **SQL injection**. SQL injection occurs when malicious input, designed to look like SQL code, is executed by the database. This could potentially allow attackers to read, modify, or delete sensitive data, or even gain control over the database server. Always, always use parameterized queries when dealing with any user input to your database.",
  "estimatedDurationSeconds": 75,
  "speakingStyle": "Informative and engaging",
  "deliveryNotes": [
    "Explain CRUD acronym",
    "Emphasize 'critical security best practice'",
    "Clearly define SQL injection",
    "Stress 'Always, always' for parameterized queries"
  ],
  "segments": [
    {
      "index": 1,
      "text": "Now that we have a connection, let's perform some basic CRUD operations. CRUD stands for Create, Read, Update, and Delete, which are the fundamental interactions you'll have with any database. To Create new data, you'll use an `INSERT` statement. To Read data, you'll use `SELECT`, often with `fetchone()` to get a single row or `fetchall()` for multiple rows. `UPDATE` statements modify existing data, and `DELETE` statements remove data.",
      "estimated_duration_seconds": 26
    },
    {
      "index": 1,
      "text": "When inserting or updating data based on user input, it's a **critical security best practice** to use *parameterized queries*, also known as prepared statements. Instead of directly embedding user-provided values into your SQL string, you use placeholders, like the question mark `?` for `sqlite3`, and pass the values as a separate tuple. This prevents a notorious vulnerability called **SQL injection**.",
      "estimated_duration_seconds": 29
    },
    {
      "index": 2,
      "text": "SQL injection occurs when malicious input, designed to look like SQL code, is executed by the database. This could potentially allow attackers to read, modify, or delete sensitive data, or even gain control over the database server. Always, always use parameterized queries when dealing with any user input to your database.",
      "estimated_duration_seconds": 20
    }
  ],
  "chapterLabel": "5.5",
  "screenLabel": "Performing CRUD: Create, Read, Update, Delete",
  "model": "gemini-2.5-flash",
  "usedKeyIndex": 0
}