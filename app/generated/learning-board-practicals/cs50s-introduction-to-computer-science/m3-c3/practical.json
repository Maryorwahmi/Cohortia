{
  "courseId": "cs50s-introduction-to-computer-science",
  "moduleNumber": 3,
  "chapterNumber": 3,
  "practical": {
    "schemaVersion": 1,
    "practicalId": "practical-cs50s-introduction-to-computer-science-m3-c3",
    "source": {
      "sourceKey": "cs50s-introduction-to-computer-science/3/3",
      "category": "Terminal Coding Lab",
      "sourcePath": "beginner/programming-software-development-fundamentals/cs50s-introduction-to-computer-science.md",
      "sourceHash": "sha256:0a148a6fefa360ef889a72c6b400512f45e9a1e10a353adf571883c66e2b5a08"
    },
    "category": "Terminal Coding Lab",
    "title": "Recursive Binary Search — Intensive Lab",
    "summary": "",
    "level": "Beginner",
    "labType": "database",
    "mode": "database_lab",
    "language": "sqlite",
    "sourceActivity": "Implement Recursive Binary Search",
    "objectives": [],
    "learningObjectives": [],
    "prerequisites": [],
    "instructions": "**Activity: Implement Recursive Binary Search**\n\nYour task is to implement a recursive version of the binary search algorithm in C. Remember that a recursive function needs a base case (when to stop) and a recursive step (how to break down the problem).\n\n**Starter Code (C):**\n```c\n#include <stdio.h>\n\n// Function to perform recursive binary search\n// arr: the sorted array\n// low: the starting index of the current search interval\n// high: the ending index of the current search interval\n// target: the value to search for\nint binary_search_recursive(int arr[], int low, int high, int target) {\n    // Base case: If the search interval is empty, the target is not found.\n    if (low > high) {\n        return -1;\n    }\n\n    int mid = low + (high - low) / 2;\n\n    // Check if target is present at mid\n    if (arr[mid] == target) {\n        return mid;\n    }\n\n    // If target is greater, search in the right half\n    if (arr[mid] < target) {\n        // TODO: Call binary_search_recursive for the right half\n        return binary_search_recursive(arr, mid + 1, high, target);\n    }\n    // If target is smaller, search in the left half\n    else {\n        // TODO: Call binary_search_recursive for the left half\n        return binary_search_recursive(arr, low, mid - 1, target);\n    }\n}\n\nint main() {\n    int sorted_arr[] = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n    int n = sizeof(sorted_arr) / sizeof(sorted_arr[0]);\n    int target1 = 38;\n    int target2 = 1;\n\n    int result1 = binary_search_recursive(sorted_arr, 0, n - 1, target1);\n    if (result1 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target1, result1);\n    } else {\n        printf(\"Target %d not found.\\n\", target1);\n    }\n\n    int result2 = binary_search_recursive(sorted_arr, 0, n - 1, target2);\n    if (result2 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target2, result2);\n    } else {\n        printf(\"Target \\\"%d\\\" not found.\\n\", target2); // Corrected print statement for not found\n    }\n\n    return 0;\n}\n```",
    "narratorGuide": "Welcome to Recursive Binary Search — Intensive Lab. I am your teacher for this practical, and we will work through Implement Recursive Binary Search together. First, read the task and predict what you expect to happen before changing anything. Then make one small change at a time and observe the result. If the result surprises you, that is useful evidence: pause, explain what changed, and try again rather than guessing. Keep the key idea in mind as you work: **Activity: Implement Recursive Binary Search**\n\nYour task is to implement a recursive version of the binary search algorithm in C. Remember that a recursive function needs a base case (when to stop) and a recursive step (how to break down the problem).\n\n**Starter Code (C):**\n```c\n#include <stdio.h>\n\n// Function to perform recursive binary search\n// arr: the sorted array\n// low: the starting index of the current search interval\n// high: the ending index of the current search interval\n// target: the value to search for\nint binary_search_recursive(int arr[], int low, int high, int target) {\n    // Base case: If the search interval is empty, the target is not found.\n    if (low > high) {\n        return -1;\n    }\n\n    int mid = low + (high - low) / 2;\n\n    // Check if target is present at mid\n    if (arr[mid] == target) {\n        return mid;\n    }\n\n    // If target is greater, search in the right half\n    if (arr[mid] < target) {\n        // TODO: Call binary_search_recursive for the right half\n        return binary_search_recursive(arr, mid + 1, high, target);\n    }\n    // If target is smaller, search in the left half\n    else {\n        // TODO: Call binary_search_recursive for the left half\n        return binary_search_recursive(arr, low, mid - 1, target);\n    }\n}\n\nint main() {\n    int sorted_arr[] = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n    int n = sizeof(sorted_arr) / sizeof(sorted_arr[0]);\n    int target1 = 38;\n    int target2 = 1;\n\n    int result1 = binary_search_recursive(sorted_arr, 0, n - 1, target1);\n    if (result1 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target1, result1);\n    } else {\n        printf(\"Target %d not found.\\n\", target1);\n    }\n\n    int result2 = binary_search_recursive(sorted_arr, 0, n - 1, target2);\n    if (result2 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target2, result2);\n    } else {\n        printf(\"Target \\\"%d\\\" not found.\\n\", target2); // Corrected print statement for not found\n    }\n\n    return 0;\n}\n```. At each checkpoint, tell yourself what the evidence proves and what it does not prove. I will guide you from observation to modification, experimentation, and verification. Take your time, use the hints only when you are stuck, and finish by explaining the solution in your own words.",
    "teacher": {
      "role": "supportive computer science teacher",
      "opening": "Welcome to Recursive Binary Search — Intensive Lab. I am your teacher for this practical, and we will work through Implement Recursive Binary Search together. First, read the task and predict what you expect to happen before changing anything. Then make one small change at a time and observe the result. If the result surprises you, that is useful evidence: pause, explain what changed, and try again rather than guessing. Keep the key idea in mind as you work: **Activity: Implement Recursive Binary Search**\n\nYour task is to implement a recursive version of the binary search algorithm in C. Remember that a recursive function needs a base case (when to stop) and a recursive step (how to break down the problem).\n\n**Starter Code (C):**\n```c\n#include <stdio.h>\n\n// Function to perform recursive binary search\n// arr: the sorted array\n// low: the starting index of the current search interval\n// high: the ending index of the current search interval\n// target: the value to search for\nint binary_search_recursive(int arr[], int low, int high, int target) {\n    // Base case: If the search interval is empty, the target is not found.\n    if (low > high) {\n        return -1;\n    }\n\n    int mid = low + (high - low) / 2;\n\n    // Check if target is present at mid\n    if (arr[mid] == target) {\n        return mid;\n    }\n\n    // If target is greater, search in the right half\n    if (arr[mid] < target) {\n        // TODO: Call binary_search_recursive for the right half\n        return binary_search_recursive(arr, mid + 1, high, target);\n    }\n    // If target is smaller, search in the left half\n    else {\n        // TODO: Call binary_search_recursive for the left half\n        return binary_search_recursive(arr, low, mid - 1, target);\n    }\n}\n\nint main() {\n    int sorted_arr[] = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n    int n = sizeof(sorted_arr) / sizeof(sorted_arr[0]);\n    int target1 = 38;\n    int target2 = 1;\n\n    int result1 = binary_search_recursive(sorted_arr, 0, n - 1, target1);\n    if (result1 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target1, result1);\n    } else {\n        printf(\"Target %d not found.\\n\", target1);\n    }\n\n    int result2 = binary_search_recursive(sorted_arr, 0, n - 1, target2);\n    if (result2 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target2, result2);\n    } else {\n        printf(\"Target \\\"%d\\\" not found.\\n\", target2); // Corrected print statement for not found\n    }\n\n    return 0;\n}\n```. At each checkpoint, tell yourself what the evidence proves and what it does not prove. I will guide you from observation to modification, experimentation, and verification. Take your time, use the hints only when you are stuck, and finish by explaining the solution in your own words.",
      "coachingPrompts": [
        "What do you predict will happen before you run it?",
        "What evidence shows that your change worked?",
        "Can you explain the result in your own words?"
      ]
    },
    "codeWalkthrough": [],
    "completionRule": "all_tests_pass",
    "environment": {
      "adapter": "database-sandbox",
      "runtime": "unspecified",
      "workingDirectory": "/workspace",
      "requiredTools": [],
      "network": "disabled",
      "resourceLimits": {
        "timeoutSeconds": 60,
        "memoryMb": 256,
        "outputKb": 64
      },
      "resetStrategy": "fresh-workspace"
    },
    "files": [
      {
        "path": "courses.sql",
        "content": "CREATE TABLE courses (\n  id INTEGER PRIMARY KEY AUTOINCREMENT,\n  title TEXT NOT NULL,\n  department TEXT NOT NULL,\n  credits INTEGER NOT NULL CHECK (credits BETWEEN 1 AND 6)\n);\n\nINSERT INTO courses (title, department, credits) VALUES\n  ('Introduction to Databases', 'Computer Science', 3),\n  ('Calculus I', 'Mathematics', 4),\n  ('Art History 101', 'Fine Arts', 3);\n\nSELECT id, title, department, credits FROM courses ORDER BY id;\n",
        "language": "sql",
        "description": "SQLite starter script for the practical.",
        "editable": true
      }
    ],
    "tasks": [
      {
        "id": "task-1",
        "title": "Recursive Binary Search — Intensive Lab",
        "instruction": "**Activity: Implement Recursive Binary Search**\n\nYour task is to implement a recursive version of the binary search algorithm in C. Remember that a recursive function needs a base case (when to stop) and a recursive step (how to break down the problem).\n\n**Starter Code (C):**\n```c\n#include <stdio.h>\n\n// Function to perform recursive binary search\n// arr: the sorted array\n// low: the starting index of the current search interval\n// high: the ending index of the current search interval\n// target: the value to search for\nint binary_search_recursive(int arr[], int low, int high, int target) {\n    // Base case: If the search interval is empty, the target is not found.\n    if (low > high) {\n        return -1;\n    }\n\n    int mid = low + (high - low) / 2;\n\n    // Check if target is present at mid\n    if (arr[mid] == target) {\n        return mid;\n    }\n\n    // If target is greater, search in the right half\n    if (arr[mid] < target) {\n        // TODO: Call binary_search_recursive for the right half\n        return binary_search_recursive(arr, mid + 1, high, target);\n    }\n    // If target is smaller, search in the left half\n    else {\n        // TODO: Call binary_search_recursive for the left half\n        return binary_search_recursive(arr, low, mid - 1, target);\n    }\n}\n\nint main() {\n    int sorted_arr[] = {2, 5, 8, 12, 16, 23, 38, 56, 72, 91};\n    int n = sizeof(sorted_arr) / sizeof(sorted_arr[0]);\n    int target1 = 38;\n    int target2 = 1;\n\n    int result1 = binary_search_recursive(sorted_arr, 0, n - 1, target1);\n    if (result1 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target1, result1);\n    } else {\n        printf(\"Target %d not found.\\n\", target1);\n    }\n\n    int result2 = binary_search_recursive(sorted_arr, 0, n - 1, target2);\n    if (result2 != -1) {\n        printf(\"Target %d found at index %d.\\n\", target2, result2);\n    } else {\n        printf(\"Target \\\"%d\\\" not found.\\n\", target2); // Corrected print statement for not found\n    }\n\n    return 0;\n}\n```",
        "narratorGuide": "In this observe step, focus on Recursive Binary Search — Intensive Lab in Recursive Binary Search — Intensive Lab. Read the instruction aloud, make a prediction, and then work carefully through one change at a time. Notice the evidence produced by your program or design. Ask yourself why the result makes sense, what assumption you tested, and what you would change next. If you get stuck, compare the result with the core concept rather than copying a solution. When the check passes, explain the reason in your own words before continuing.",
        "required": true,
        "requiredConcepts": [],
        "hints": [],
        "checkIds": [
          "starter-sql-present"
        ],
        "animationTimeline": [],
        "totalDurationMs": 180000,
        "terminalCommands": []
      }
    ],
    "checks": [
      {
        "id": "starter-sql-present",
        "type": "file",
        "adapter": "database-sandbox",
        "timeoutSeconds": 30,
        "visibility": "learner",
        "explanation": "The learner has a SQL starter script in the workspace."
      },
      {
        "id": "database-schema-and-data",
        "type": "sql",
        "adapter": "database-sandbox",
        "timeoutSeconds": 30,
        "visibility": "learner",
        "explanation": "The courses table contains at least the three starter rows.",
        "command": "SELECT COUNT(*) FROM courses;",
        "passCondition": {
          "expected": "3"
        },
        "matchMode": "contains"
      }
    ],
    "hints": [],
    "evidence": [
      {
        "id": "practical-result",
        "type": "artifact",
        "label": "Practical result",
        "required": true,
        "redactSecrets": true
      }
    ],
    "safety": {
      "profile": "database-isolated",
      "networkAccess": "disabled",
      "hostAccess": "none",
      "requiresManualReview": true,
      "prohibitedOperations": [
        "accessing the host filesystem",
        "reading credentials or secrets",
        "privileged operations",
        "network access outside declared fixtures"
      ],
      "learnerWarning": "This practical runs in an isolated workspace. Do not use real credentials or production resources."
    },
    "cleanup": {
      "strategy": "discard-workspace",
      "instructions": "Discard the isolated workspace after completion."
    },
    "completionRules": {
      "requiredChecks": [
        "starter-sql-present",
        "database-schema-and-data"
      ],
      "minimumScore": 1,
      "requiresEvidence": true,
      "manualReview": true
    },
    "generator": {
      "generatorVersion": "phase4.2",
      "classifierVersion": "phase3.1"
    },
    "metadata": {
      "courseId": "cs50s-introduction-to-computer-science",
      "moduleNumber": 3,
      "chapterNumber": 3,
      "model": "gemini-2.5-flash",
      "generatedAt": "2026-09-24T20:38:30.554Z",
      "activitySourcePath": "beginner/programming-software-development-fundamentals/cs50s-introduction-to-computer-science.md",
      "lessonSourcePath": "beginner/programming-software-development-fundamentals/cs50s-introduction-to-computer-science.md",
      "activitySourceHash": "sha256:0a148a6fefa360ef889a72c6b400512f45e9a1e10a353adf571883c66e2b5a08",
      "lessonSourceHash": "sha256:3d06a30bf934a1976deb739f3e00e855fdb3dd5fab759e225433dc41ecb785f8"
    },
    "animationMetadata": {
      "type": "interactive-code-reveal",
      "narrator": "educator",
      "synchronizedPlayback": true,
      "totalLessonDurationMs": 180000
    }
  }
}