{
  "scope": "chapter",
  "title": "5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE",
  "summary": "This chapter introduces Data Manipulation Language (DML) in SQL, focusing on the essential commands INSERT, UPDATE, and DELETE. Learners will master adding new records, modifying existing data, and removing rows from database tables. Crucial emphasis is placed on understanding and implementing the WHERE clause for precise targeting, mitigating the risks of accidental data loss. The module also provides an introductory overview of SQL transactions, explaining how they ensure data integrity through atomicity, commit, and rollback operations. Practical hands-on activities reinforce safe and effective data management techniques.",
  "estimated_total_duration_seconds": 510,
  "screens": [
    {
      "screen": 1,
      "type": "intro",
      "title": "SQL Data Manipulation: INSERT, UPDATE, DELETE",
      "content": "So far, we've focused on retrieving data from a database using `SELECT`. But what about getting data *into* the database, changing existing data, or removing data that's no longer needed? This is where Data Manipulation Language (DML) commands come into play. The three primary DML commands are `INSERT`, `UPDATE`, and `DELETE`. These commands allow you to manage the lifecycle of data within your tables. In this lesson, we'll learn to write `INSERT` statements to add new rows, construct `UPDATE` statements to modify existing data, and formulate `DELETE` statements to remove records. Crucially, we'll explore the potential dangers of `UPDATE` and `DELETE` without a `WHERE` clause and how to prevent accidental data loss. Finally, we'll touch upon the basic concept of a transaction, a powerful feature for ensuring data integrity during complex modifications.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** intro\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nSo far, we've focused on retrieving data from a database using `SELECT`. But what about getting data *into* the database, changing existing data, or removing data that's no longer needed? This is where Data Manipulation Language (DML) commands come into play. The three primary DML commands are `INSERT`, `UPDATE`, and `DELETE`. These commands allow you to manage the lifecycle of data within your tables. In this lesson, we'll learn to write `INSERT` statements to add new rows, construct `UPDATE` statements to modify existing data, and formulate `DELETE` statements to remove records. Crucially, we'll explore the potential dangers of `UPDATE` and `DELETE` without a `WHERE` clause and how to prevent accidental data loss. Finally, we'll touch upon the basic concept of a transaction, a powerful feature for ensuring data integrity during complex modifications.\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: So far, we've focused on retrieving data from a database using `SELECT`. But what about getting data *into* the database, changing existing data, or removing data that's no longer needed? This is where Data Manipulation Language (DML) commands come into play. The three primary DML commands are `INSERT`, `UPDATE`, and `DELETE`. These commands allow you to manage the lifecycle of data within your tables. In this lesson, we'll learn to write `INSERT` statements to add new rows, construct `UPDATE` statements to modify existing data, and formulate `DELETE` statements to remove records. Crucially, we'll explore the potential dangers of `UPDATE` and `DELETE` without a `WHERE` clause and how to prevent accidental data loss. Finally, we'll touch upon the basic concept of a transaction, a powerful feature for ensuring data integrity during complex modifications.. Narration focus: Welcome to this chapter on SQL Data Manipulation, where we'll move beyond just querying data and learn how to actively manage it. So far, we've focused on retrieving data using SELECT statements. But what happens when you need to add new information, modify existing records, or remove outdated entries from your database? That's precisely what Data Manipulation Language, or DML, is for. In this lesson, we'll dive into the three core DML commands: INSERT, UPDATE, and DELETE. By the end, you'll be able to write INSERT statements to add new rows, construct UPDATE statements to modify existing data, and formulate DELETE statements to remove records. Crucially, we'll also explore the potential dangers of UPDATE and DELETE without a WHERE clause, and how to prevent accidental data loss. Finally, we'll touch upon the basic concept of a transaction, a powerful feature for ensuring data integrity during complex modifications. Let's get started!.\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: Welcome to this chapter on SQL Data Manipulation, where we'll move beyond just querying data and learn how to actively manage it. So far, we've focused on retrieving data using SELECT statements. But what happens when you need to add new information, modify existing records, or remove outdated entries from your database? That's precisely what Data Manipulation Language, or DML, is for. In this lesson, we'll dive into the three core DML commands: INSERT, UPDATE, and DELETE. By the end, you'll be able to write INSERT statements to add new rows, construct UPDATE statements to modify existing data, and formulate DELETE statements to remove records. Crucially, we'll also explore the potential dangers of UPDATE and DELETE without a WHERE clause, and how to prevent accidental data loss. Finally, we'll touch upon the basic concept of a transaction, a powerful feature for ensuring data integrity during complex modifications. Let's get started!.",
      "narration_script": "Welcome to this chapter on SQL Data Manipulation, where we'll move beyond just querying data and learn how to actively manage it. So far, we've focused on retrieving data using SELECT statements. But what happens when you need to add new information, modify existing records, or remove outdated entries from your database? That's precisely what Data Manipulation Language, or DML, is for. In this lesson, we'll dive into the three core DML commands: INSERT, UPDATE, and DELETE. By the end, you'll be able to write INSERT statements to add new rows, construct UPDATE statements to modify existing data, and formulate DELETE statements to remove records. Crucially, we'll also explore the potential dangers of UPDATE and DELETE without a WHERE clause, and how to prevent accidental data loss. Finally, we'll touch upon the basic concept of a transaction, a powerful feature for ensuring data integrity during complex modifications. Let's get started!",
      "estimated_duration_seconds": 45,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Welcome to this chapter on SQL Data Manipulation, where we'll move beyond just querying data and learn how to actively manage it.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 1,
          "text": "So far, we've focused on retrieving data using SELECT statements.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 2,
          "text": "But what happens when you need to add new information, modify existing records, or remove outdated entries from your database?",
          "estimated_duration_seconds": 10
        },
        {
          "index": 3,
          "text": "That's precisely what Data Manipulation Language, or DML, is for.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 4,
          "text": "In this lesson, we'll dive into the three core DML commands: INSERT, UPDATE, and DELETE.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 5,
          "text": "By the end, you'll be able to write INSERT statements to add new rows, construct UPDATE statements to modify existing data, and formulate DELETE statements to remove records.",
          "estimated_duration_seconds": 15
        },
        {
          "index": 6,
          "text": "Crucially, we'll also explore the potential dangers of UPDATE and DELETE without a WHERE clause, and how to prevent accidental data loss.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 7,
          "text": "Finally, we'll touch upon the basic concept of a transaction, a powerful feature for ensuring data integrity during complex modifications.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 8,
          "text": "Let's get started!",
          "estimated_duration_seconds": 3
        }
      ]
    },
    {
      "screen": 2,
      "type": "concept",
      "title": "Adding New Data: The INSERT Statement",
      "content": "Let's start with `INSERT`, which is used to add new rows (records) to a table. When you create a new student, a new product, or a new order, you'll use `INSERT`. The basic syntax requires you to specify the table name and the values for each column. There are two main ways to use `INSERT`. First, inserting values for all columns: If you are providing values for every column in the table, in the order they were defined during table creation, you can omit the column names. Suppose we have a `Students` table with columns `StudentID`, `FirstName`, `LastName`, `Major`, `GPA`. Example: `INSERT INTO Students VALUES (1001, 'Alice', 'Smith', 'Computer Science', 3.9);`. Common Mistake: Forgetting to provide values for all columns or providing them in the wrong order. This will likely result in an error or data being inserted into the wrong columns. It's generally safer to explicitly list the columns.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** concept\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nLet's start with `INSERT`, which is used to add new rows (records) to a table. When you create a new student, a new product, or a new order, you'll use `INSERT`. The basic syntax requires you to specify the table name and the values for each column. There are two main ways to use `INSERT`. First, inserting values for all columns: If you are providing values for every column in the table, in the order they were defined during table creation, you can omit the column names. Suppose we have a `Students` table with columns `StudentID`, `FirstName`, `LastName`, `Major`, `GPA`. Example: `INSERT INTO Students VALUES (1001, 'Alice', 'Smith', 'Computer Science', 3.9);`. Common Mistake: Forgetting to provide values for all columns or providing them in the wrong order. This will likely result in an error or data being inserted into the wrong columns. It's generally safer to explicitly list the columns.\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: Let's start with `INSERT`, which is used to add new rows (records) to a table. When you create a new student, a new product, or a new order, you'll use `INSERT`. The basic syntax requires you to specify the table name and the values for each column. There are two main ways to use `INSERT`. First, inserting values for all columns: If you are providing values for every column in the table, in the order they were defined during table creation, you can omit the column names. Suppose we have a `Students` table with columns `StudentID`, `FirstName`, `LastName`, `Major`, `GPA`. Example: `INSERT INTO Students VALUES (1001, 'Alice', 'Smith', 'Computer Science', 3.9);`. Common Mistake: Forgetting to provide values for all columns or providing them in the wrong order. This will likely result in an error or data being inserted into the wrong columns. It's generally safer to explicitly list the columns.. Narration focus: Let's begin with the INSERT statement, your tool for adding new rows, or records, into a table. Think of it like adding a new entry to a spreadsheet. When you register a new user, add a product to an inventory, or record a new transaction, INSERT is the command you'll use. The basic syntax is quite straightforward: INSERT INTO followed by the table name, and then VALUES followed by the data for each column. If you're providing values for *every* column in the table, and in the *exact order* they were defined when the table was created, you can actually omit listing the column names. For example, if we have a Students table with StudentID, FirstName, LastName, Major, and GPA columns, we could add a new student, Alice Smith, like this: INSERT INTO Students VALUES (1001, 'Alice', 'Smith', 'Computer Science', 3.9); Here, each value directly corresponds to a column in sequence. However, a common mistake is forgetting a value or getting the order wrong, which can lead to errors or incorrect data. This is why explicitly listing columns, as we'll see next, is often a safer and more robust approach..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: Let's begin with the INSERT statement, your tool for adding new rows, or records, into a table. Think of it like adding a new entry to a spreadsheet. When you register a new user, add a product to an inventory, or record a new transaction, INSERT is the command you'll use. The basic syntax is quite straightforward: INSERT INTO followed by the table name, and then VALUES followed by the data for each column. If you're providing values for *every* column in the table, and in the *exact order* they were defined when the table was created, you can actually omit listing the column names. For example, if we have a Students table with StudentID, FirstName, LastName, Major, and GPA columns, we could add a new student, Alice Smith, like this: INSERT INTO Students VALUES (1001, 'Alice', 'Smith', 'Computer Science', 3.9); Here, each value directly corresponds to a column in sequence. However, a common mistake is forgetting a value or getting the order wrong, which can lead to errors or incorrect data. This is why explicitly listing columns, as we'll see next, is often a safer and more robust approach..",
      "narration_script": "Let's begin with the INSERT statement, your tool for adding new rows, or records, into a table. Think of it like adding a new entry to a spreadsheet. When you register a new user, add a product to an inventory, or record a new transaction, INSERT is the command you'll use. The basic syntax is quite straightforward: INSERT INTO followed by the table name, and then VALUES followed by the data for each column. If you're providing values for *every* column in the table, and in the *exact order* they were defined when the table was created, you can actually omit listing the column names. For example, if we have a Students table with StudentID, FirstName, LastName, Major, and GPA columns, we could add a new student, Alice Smith, like this: INSERT INTO Students VALUES (1001, 'Alice', 'Smith', 'Computer Science', 3.9); Here, each value directly corresponds to a column in sequence. However, a common mistake is forgetting a value or getting the order wrong, which can lead to errors or incorrect data. This is why explicitly listing columns, as we'll see next, is often a safer and more robust approach.",
      "estimated_duration_seconds": 60,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Let's begin with the INSERT statement, your tool for adding new rows, or records, into a table.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 1,
          "text": "Think of it like adding a new entry to a spreadsheet.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 2,
          "text": "When you register a new user, add a product to an inventory, or record a new transaction, INSERT is the command you'll use.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 3,
          "text": "The basic syntax is quite straightforward: INSERT INTO followed by the table name, and then VALUES followed by the data for each column.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 4,
          "text": "If you're providing values for *every* column in the table, and in the *exact order* they were defined when the table was created, you can actually omit listing the column names.",
          "estimated_duration_seconds": 14
        },
        {
          "index": 5,
          "text": "For example, if we have a Students table with StudentID, FirstName, LastName, Major, and GPA columns, we could add a new student, Alice Smith, like this: INSERT INTO Students VALUES (1001, 'Alice', 'Smith', 'Computer Science', 3.9);",
          "estimated_duration_seconds": 18
        },
        {
          "index": 6,
          "text": "Here, each value directly corresponds to a column in sequence.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 7,
          "text": "However, a common mistake is forgetting a value or getting the order wrong, which can lead to errors or incorrect data.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 8,
          "text": "This is why explicitly listing columns, as we'll see next, is often a safer and more robust approach.",
          "estimated_duration_seconds": 10
        }
      ]
    },
    {
      "screen": 3,
      "type": "concept",
      "title": "Targeted Inserts and Common Pitfalls",
      "content": "Inserting values for specific columns: This is often the preferred method, as it makes your query more robust to future schema changes (e.g., adding a new column with a default value). You explicitly list the columns you are providing values for. Example: `INSERT INTO Students (StudentID, FirstName, LastName, Major, GPA) VALUES (1002, 'Bob', 'Johnson', 'Mathematics', 3.7);`. If a column has a default value or allows `NULL` and you don't list it, the database will use the default or `NULL` respectively. For example, if `GPA` could be `NULL` initially: `INSERT INTO Students (StudentID, FirstName, LastName, Major) VALUES (1003, 'Charlie', 'Brown', 'History');` Here, `GPA` for Charlie Brown would be `NULL` (if allowed by the schema) or its default value.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** concept\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nInserting values for specific columns: This is often the preferred method, as it makes your query more robust to future schema changes (e.g., adding a new column with a default value). You explicitly list the columns you are providing values for. Example: `INSERT INTO Students (StudentID, FirstName, LastName, Major, GPA) VALUES (1002, 'Bob', 'Johnson', 'Mathematics', 3.7);`. If a column has a default value or allows `NULL` and you don't list it, the database will use the default or `NULL` respectively. For example, if `GPA` could be `NULL` initially: `INSERT INTO Students (StudentID, FirstName, LastName, Major) VALUES (1003, 'Charlie', 'Brown', 'History');` Here, `GPA` for Charlie Brown would be `NULL` (if allowed by the schema) or its default value.\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: Inserting values for specific columns: This is often the preferred method, as it makes your query more robust to future schema changes (e.g., adding a new column with a default value). You explicitly list the columns you are providing values for. Example: `INSERT INTO Students (StudentID, FirstName, LastName, Major, GPA) VALUES (1002, 'Bob', 'Johnson', 'Mathematics', 3.7);`. If a column has a default value or allows `NULL` and you don't list it, the database will use the default or `NULL` respectively. For example, if `GPA` could be `NULL` initially: `INSERT INTO Students (StudentID, FirstName, LastName, Major) VALUES (1003, 'Charlie', 'Brown', 'History');` Here, `GPA` for Charlie Brown would be `NULL` (if allowed by the schema) or its default value.. Narration focus: While inserting values for all columns works, the preferred and more robust method is to explicitly list the columns you're providing values for. This makes your query much more resilient to future changes in your table's schema, such as adding a new column with a default value. You simply specify the column names in parentheses after the table name, and then provide the corresponding values in the VALUES clause. For instance, to add Bob Johnson, we'd write: INSERT INTO Students (StudentID, FirstName, LastName, Major, GPA) VALUES (1002, 'Bob', 'Johnson', 'Mathematics', 3.7); Notice how the column list directly matches the order of values. What if a column has a default value or allows NULL and you don't list it? The database will automatically use the default value, or NULL if allowed. For example, if GPA could be NULL initially, you could add Charlie Brown like this: INSERT INTO Students (StudentID, FirstName, LastName, Major) VALUES (1003, 'Charlie', 'Brown', 'History'); Here, GPA for Charlie would be NULL (if allowed by the schema) or its default. This explicit approach prevents errors and makes your code clearer, especially when dealing with tables that have many columns or evolving schemas..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: While inserting values for all columns works, the preferred and more robust method is to explicitly list the columns you're providing values for. This makes your query much more resilient to future changes in your table's schema, such as adding a new column with a default value. You simply specify the column names in parentheses after the table name, and then provide the corresponding values in the VALUES clause. For instance, to add Bob Johnson, we'd write: INSERT INTO Students (StudentID, FirstName, LastName, Major, GPA) VALUES (1002, 'Bob', 'Johnson', 'Mathematics', 3.7); Notice how the column list directly matches the order of values. What if a column has a default value or allows NULL and you don't list it? The database will automatically use the default value, or NULL if allowed. For example, if GPA could be NULL initially, you could add Charlie Brown like this: INSERT INTO Students (StudentID, FirstName, LastName, Major) VALUES (1003, 'Charlie', 'Brown', 'History'); Here, GPA for Charlie would be NULL (if allowed by the schema) or its default. This explicit approach prevents errors and makes your code clearer, especially when dealing with tables that have many columns or evolving schemas..",
      "narration_script": "While inserting values for all columns works, the preferred and more robust method is to explicitly list the columns you're providing values for. This makes your query much more resilient to future changes in your table's schema, such as adding a new column with a default value. You simply specify the column names in parentheses after the table name, and then provide the corresponding values in the VALUES clause. For instance, to add Bob Johnson, we'd write: INSERT INTO Students (StudentID, FirstName, LastName, Major, GPA) VALUES (1002, 'Bob', 'Johnson', 'Mathematics', 3.7); Notice how the column list directly matches the order of values. What if a column has a default value or allows NULL and you don't list it? The database will automatically use the default value, or NULL if allowed. For example, if GPA could be NULL initially, you could add Charlie Brown like this: INSERT INTO Students (StudentID, FirstName, LastName, Major) VALUES (1003, 'Charlie', 'Brown', 'History'); Here, GPA for Charlie would be NULL (if allowed by the schema) or its default. This explicit approach prevents errors and makes your code clearer, especially when dealing with tables that have many columns or evolving schemas.",
      "estimated_duration_seconds": 60,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "While inserting values for all columns works, the preferred and more robust method is to explicitly list the columns you're providing values for.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 1,
          "text": "This makes your query much more resilient to future changes in your table's schema, such as adding a new column with a default value.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 2,
          "text": "You simply specify the column names in parentheses after the table name, and then provide the corresponding values in the VALUES clause.",
          "estimated_duration_seconds": 11
        },
        {
          "index": 3,
          "text": "For instance, to add Bob Johnson, we'd write: INSERT INTO Students (StudentID, FirstName, LastName, Major, GPA) VALUES (1002, 'Bob', 'Johnson', 'Mathematics', 3.7);",
          "estimated_duration_seconds": 15
        },
        {
          "index": 4,
          "text": "Notice how the column list directly matches the order of values.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 5,
          "text": "What if a column has a default value or allows NULL and you don't list it?",
          "estimated_duration_seconds": 7
        },
        {
          "index": 6,
          "text": "The database will automatically use the default value, or NULL if allowed.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 7,
          "text": "For example, if GPA could be NULL initially, you could add Charlie Brown like this: INSERT INTO Students (StudentID, FirstName, LastName, Major) VALUES (1003, 'Charlie', 'Brown', 'History');",
          "estimated_duration_seconds": 15
        },
        {
          "index": 8,
          "text": "Here, GPA for Charlie would be NULL (if allowed by the schema) or its default.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 9,
          "text": "This explicit approach prevents errors and makes your code clearer, especially when dealing with tables that have many columns or evolving schemas.",
          "estimated_duration_seconds": 13
        }
      ]
    },
    {
      "screen": 4,
      "type": "concept",
      "title": "Modifying Existing Data: The UPDATE Statement",
      "content": "Next, we have `UPDATE`, used to modify existing data in one or more rows. This is incredibly useful for correcting errors, changing statuses, or updating information. The `UPDATE` statement consists of the `UPDATE` keyword, the table name, the `SET` keyword followed by the column(s) to change and their new values, and most critically, the `WHERE` clause. Let's say Alice Smith's major changed to 'Data Science': `UPDATE Students SET Major = 'Data Science' WHERE StudentID = 1001;` The `WHERE` clause here is paramount. It specifies *which* rows should be updated. In this case, only the student with `StudentID` 1001 will have their `Major` changed. You can update multiple columns at once: `UPDATE Students SET Major = 'Data Science', GPA = 4.0 WHERE StudentID = 1001;`",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** concept\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nNext, we have `UPDATE`, used to modify existing data in one or more rows. This is incredibly useful for correcting errors, changing statuses, or updating information. The `UPDATE` statement consists of the `UPDATE` keyword, the table name, the `SET` keyword followed by the column(s) to change and their new values, and most critically, the `WHERE` clause. Let's say Alice Smith's major changed to 'Data Science': `UPDATE Students SET Major = 'Data Science' WHERE StudentID = 1001;` The `WHERE` clause here is paramount. It specifies *which* rows should be updated. In this case, only the student with `StudentID` 1001 will have their `Major` changed. You can update multiple columns at once: `UPDATE Students SET Major = 'Data Science', GPA = 4.0 WHERE StudentID = 1001;`\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: Next, we have `UPDATE`, used to modify existing data in one or more rows. This is incredibly useful for correcting errors, changing statuses, or updating information. The `UPDATE` statement consists of the `UPDATE` keyword, the table name, the `SET` keyword followed by the column(s) to change and their new values, and most critically, the `WHERE` clause. Let's say Alice Smith's major changed to 'Data Science': `UPDATE Students SET Major = 'Data Science' WHERE StudentID = 1001;` The `WHERE` clause here is paramount. It specifies *which* rows should be updated. In this case, only the student with `StudentID` 1001 will have their `Major` changed. You can update multiple columns at once: `UPDATE Students SET Major = 'Data Science', GPA = 4.0 WHERE StudentID = 1001;`. Narration focus: Now that we know how to add data, let's learn how to change it using the UPDATE statement. This command is essential for correcting errors, updating statuses, or modifying any existing information in your database. The UPDATE statement requires three key components: first, the UPDATE keyword followed by the table name; second, the SET keyword, which specifies *which* column or columns you want to change and their *new* values; and third, and most critically, the WHERE clause, which determines *which specific rows* will be affected by the update. Let's imagine Alice Smith changed her major. To update her record, we would write: UPDATE Students SET Major = 'Data Science' WHERE StudentID = 1001; Here, the WHERE StudentID = 1001 clause ensures that only Alice's record is modified. Without it, every student's major would change to 'Data Science'! You can also update multiple columns at once, like this: UPDATE Students SET Major = 'Data Science', GPA = 4.0 WHERE StudentID = 1001; Always remember, the WHERE clause is your precision tool; it scopes your changes to exactly where they're needed..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: Now that we know how to add data, let's learn how to change it using the UPDATE statement. This command is essential for correcting errors, updating statuses, or modifying any existing information in your database. The UPDATE statement requires three key components: first, the UPDATE keyword followed by the table name; second, the SET keyword, which specifies *which* column or columns you want to change and their *new* values; and third, and most critically, the WHERE clause, which determines *which specific rows* will be affected by the update. Let's imagine Alice Smith changed her major. To update her record, we would write: UPDATE Students SET Major = 'Data Science' WHERE StudentID = 1001; Here, the WHERE StudentID = 1001 clause ensures that only Alice's record is modified. Without it, every student's major would change to 'Data Science'! You can also update multiple columns at once, like this: UPDATE Students SET Major = 'Data Science', GPA = 4.0 WHERE StudentID = 1001; Always remember, the WHERE clause is your precision tool; it scopes your changes to exactly where they're needed..",
      "narration_script": "Now that we know how to add data, let's learn how to change it using the UPDATE statement. This command is essential for correcting errors, updating statuses, or modifying any existing information in your database. The UPDATE statement requires three key components: first, the UPDATE keyword followed by the table name; second, the SET keyword, which specifies *which* column or columns you want to change and their *new* values; and third, and most critically, the WHERE clause, which determines *which specific rows* will be affected by the update. Let's imagine Alice Smith changed her major. To update her record, we would write: UPDATE Students SET Major = 'Data Science' WHERE StudentID = 1001; Here, the WHERE StudentID = 1001 clause ensures that only Alice's record is modified. Without it, every student's major would change to 'Data Science'! You can also update multiple columns at once, like this: UPDATE Students SET Major = 'Data Science', GPA = 4.0 WHERE StudentID = 1001; Always remember, the WHERE clause is your precision tool; it scopes your changes to exactly where they're needed.",
      "estimated_duration_seconds": 60,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Now that we know how to add data, let's learn how to change it using the UPDATE statement.",
          "estimated_duration_seconds": 9
        },
        {
          "index": 1,
          "text": "This command is essential for correcting errors, updating statuses, or modifying any existing information in your database.",
          "estimated_duration_seconds": 11
        },
        {
          "index": 2,
          "text": "The UPDATE statement requires three key components: first, the UPDATE keyword followed by the table name; second, the SET keyword, which specifies *which* column or columns you want to change and their *new* values; and third, and most critically, the WHERE clause, which determines *which specific rows* will be affected by the update.",
          "estimated_duration_seconds": 28
        },
        {
          "index": 3,
          "text": "Let's imagine Alice Smith changed her major.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 4,
          "text": "To update her record, we would write: UPDATE Students SET Major = 'Data Science' WHERE StudentID = 1001;",
          "estimated_duration_seconds": 10
        },
        {
          "index": 5,
          "text": "Here, the WHERE StudentID = 1001 clause ensures that only Alice's record is modified.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 6,
          "text": "Without it, every student's major would change to 'Data Science'!",
          "estimated_duration_seconds": 6
        },
        {
          "index": 7,
          "text": "You can also update multiple columns at once, like this: UPDATE Students SET Major = 'Data Science', GPA = 4.0 WHERE StudentID = 1001;",
          "estimated_duration_seconds": 15
        },
        {
          "index": 8,
          "text": "Always remember, the WHERE clause is your precision tool; it scopes your changes to exactly where they're needed.",
          "estimated_duration_seconds": 12
        }
      ]
    },
    {
      "screen": 5,
      "type": "diagram",
      "title": "The Critical WHERE Clause: A Safety Net",
      "content": "CRITICAL SAFETY NOTE: If you omit the `WHERE` clause in an `UPDATE` statement, the database will attempt to update *every single row* in the table. This is almost never what you want and can lead to catastrophic data loss or corruption. Similarly, omitting the `WHERE` clause in a `DELETE` statement will result in *all rows* being deleted from the table! This is an irreversible operation in many database systems. Always, always, *always* double-check your `WHERE` clause when using `UPDATE` or `DELETE`. A common practice is to first run a `SELECT` query with the same `WHERE` clause to verify which rows would be affected before executing the `UPDATE` or `DELETE`.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** diagram\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nCRITICAL SAFETY NOTE: If you omit the `WHERE` clause in an `UPDATE` statement, the database will attempt to update *every single row* in the table. This is almost never what you want and can lead to catastrophic data loss or corruption. Similarly, omitting the `WHERE` clause in a `DELETE` statement will result in *all rows* being deleted from the table! This is an irreversible operation in many database systems. Always, always, *always* double-check your `WHERE` clause when using `UPDATE` or `DELETE`. A common practice is to first run a `SELECT` query with the same `WHERE` clause to verify which rows would be affected before executing the `UPDATE` or `DELETE`.\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: CRITICAL SAFETY NOTE: If you omit the `WHERE` clause in an `UPDATE` statement, the database will attempt to update *every single row* in the table. This is almost never what you want and can lead to catastrophic data loss or corruption. Similarly, omitting the `WHERE` clause in a `DELETE` statement will result in *all rows* being deleted from the table! This is an irreversible operation in many database systems. Always, always, *always* double-check your `WHERE` clause when using `UPDATE` or `DELETE`. A common practice is to first run a `SELECT` query with the same `WHERE` clause to verify which rows would be affected before executing the `UPDATE` or `DELETE`.. Narration focus: Now we arrive at a critical safety lesson for both UPDATE and DELETE statements. We've seen how the WHERE clause acts as a filter, ensuring your commands only affect the specific rows you intend. But what happens if you forget it? This is where extreme caution is needed. If you omit the WHERE clause in an UPDATE statement, the database will attempt to update *every single row* in the table. Imagine changing every student's major to 'Data Science' by accident! Similarly, if you run a DELETE statement without a WHERE clause, *all rows* will be removed from your table. This is equivalent to emptying the entire table, and in many systems, it's an irreversible operation outside of a transaction. To prevent such catastrophic data loss, a wise precaution is to first run a SELECT query with the *exact same* WHERE clause you plan to use for your UPDATE or DELETE. This allows you to verify *which* rows would be affected *before* you make any permanent changes. Always, always, double-check your WHERE clause..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: Now we arrive at a critical safety lesson for both UPDATE and DELETE statements. We've seen how the WHERE clause acts as a filter, ensuring your commands only affect the specific rows you intend. But what happens if you forget it? This is where extreme caution is needed. If you omit the WHERE clause in an UPDATE statement, the database will attempt to update *every single row* in the table. Imagine changing every student's major to 'Data Science' by accident! Similarly, if you run a DELETE statement without a WHERE clause, *all rows* will be removed from your table. This is equivalent to emptying the entire table, and in many systems, it's an irreversible operation outside of a transaction. To prevent such catastrophic data loss, a wise precaution is to first run a SELECT query with the *exact same* WHERE clause you plan to use for your UPDATE or DELETE. This allows you to verify *which* rows would be affected *before* you make any permanent changes. Always, always, double-check your WHERE clause..",
      "narration_script": "Now we arrive at a critical safety lesson for both UPDATE and DELETE statements. We've seen how the WHERE clause acts as a filter, ensuring your commands only affect the specific rows you intend. But what happens if you forget it? This is where extreme caution is needed. If you omit the WHERE clause in an UPDATE statement, the database will attempt to update *every single row* in the table. Imagine changing every student's major to 'Data Science' by accident! Similarly, if you run a DELETE statement without a WHERE clause, *all rows* will be removed from your table. This is equivalent to emptying the entire table, and in many systems, it's an irreversible operation outside of a transaction. To prevent such catastrophic data loss, a wise precaution is to first run a SELECT query with the *exact same* WHERE clause you plan to use for your UPDATE or DELETE. This allows you to verify *which* rows would be affected *before* you make any permanent changes. Always, always, double-check your WHERE clause.",
      "estimated_duration_seconds": 65,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Now we arrive at a critical safety lesson for both UPDATE and DELETE statements.",
          "estimated_duration_seconds": 9
        },
        {
          "index": 1,
          "text": "We've seen how the WHERE clause acts as a filter, ensuring your commands only affect the specific rows you intend.",
          "estimated_duration_seconds": 11
        },
        {
          "index": 2,
          "text": "But what happens if you forget it? This is where extreme caution is needed.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 3,
          "text": "If you omit the WHERE clause in an UPDATE statement, the database will attempt to update *every single row* in the table.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 4,
          "text": "Imagine changing every student's major to 'Data Science' by accident!",
          "estimated_duration_seconds": 6
        },
        {
          "index": 5,
          "text": "Similarly, if you run a DELETE statement without a WHERE clause, *all rows* will be removed from your table.",
          "estimated_duration_seconds": 11
        },
        {
          "index": 6,
          "text": "This is equivalent to emptying the entire table, and in many systems, it's an irreversible operation outside of a transaction.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 7,
          "text": "To prevent such catastrophic data loss, a wise precaution is to first run a SELECT query with the *exact same* WHERE clause you plan to use for your UPDATE or DELETE.",
          "estimated_duration_seconds": 18
        },
        {
          "index": 8,
          "text": "This allows you to verify *which* rows would be affected *before* you make any permanent changes.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 9,
          "text": "Always, always, double-check your WHERE clause.",
          "estimated_duration_seconds": 6
        }
      ]
    },
    {
      "screen": 6,
      "type": "diagram",
      "title": "Ensuring Data Integrity: SQL Transactions",
      "content": "These DML operations are often part of a **transaction**. A transaction is a sequence of operations performed as a single logical unit of work. For example, transferring money from one bank account to another involves deducting from one and adding to another. If either step fails, both should be undone. This 'all or nothing' property is called atomicity. In SQL, you can explicitly start a transaction (`BEGIN TRANSACTION` or `START TRANSACTION`), perform multiple `INSERT`, `UPDATE`, or `DELETE` statements, and then either `COMMIT` them (make changes permanent) or `ROLLBACK` (undo all changes since the transaction began). This provides a critical safety net for complex operations. While we won't delve deep into transactions in this introductory chapter, understanding their existence is important for robust database interactions.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** diagram\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nThese DML operations are often part of a **transaction**. A transaction is a sequence of operations performed as a single logical unit of work. For example, transferring money from one bank account to another involves deducting from one and adding to another. If either step fails, both should be undone. This 'all or nothing' property is called atomicity. In SQL, you can explicitly start a transaction (`BEGIN TRANSACTION` or `START TRANSACTION`), perform multiple `INSERT`, `UPDATE`, or `DELETE` statements, and then either `COMMIT` them (make changes permanent) or `ROLLBACK` (undo all changes since the transaction began). This provides a critical safety net for complex operations. While we won't delve deep into transactions in this introductory chapter, understanding their existence is important for robust database interactions.\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: These DML operations are often part of a **transaction**. A transaction is a sequence of operations performed as a single logical unit of work. For example, transferring money from one bank account to another involves deducting from one and adding to another. If either step fails, both should be undone. This 'all or nothing' property is called atomicity. In SQL, you can explicitly start a transaction (`BEGIN TRANSACTION` or `START TRANSACTION`), perform multiple `INSERT`, `UPDATE`, or `DELETE` statements, and then either `COMMIT` them (make changes permanent) or `ROLLBACK` (undo all changes since the transaction began). This provides a critical safety net for complex operations. While we won't delve deep into transactions in this introductory chapter, understanding their existence is important for robust database interactions.. Narration focus: Finally, let's briefly touch upon a powerful concept for ensuring data integrity: **transactions**. Imagine you're transferring money from one bank account to another. This isn't one single action; it involves deducting money from the first account *and* adding it to the second. What if the deduction succeeds but the addition fails? You'd lose money! A transaction treats a sequence of operations like these as a single, indivisible unit of work. This 'all or nothing' property is called atomicity. In SQL, you can explicitly start a transaction using BEGIN TRANSACTION or START TRANSACTION. You then perform your INSERT, UPDATE, or DELETE statements. If all operations succeed and you're happy with the changes, you COMMIT them, making them permanent in the database. But if anything goes wrong, or you decide you don't want the changes, you can ROLLBACK the transaction, which undoes all changes made since the transaction began, restoring the database to its original state. While a deep dive into transactions is beyond this introduction, understanding their existence is crucial for building robust and reliable database applications..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: Finally, let's briefly touch upon a powerful concept for ensuring data integrity: **transactions**. Imagine you're transferring money from one bank account to another. This isn't one single action; it involves deducting money from the first account *and* adding it to the second. What if the deduction succeeds but the addition fails? You'd lose money! A transaction treats a sequence of operations like these as a single, indivisible unit of work. This 'all or nothing' property is called atomicity. In SQL, you can explicitly start a transaction using BEGIN TRANSACTION or START TRANSACTION. You then perform your INSERT, UPDATE, or DELETE statements. If all operations succeed and you're happy with the changes, you COMMIT them, making them permanent in the database. But if anything goes wrong, or you decide you don't want the changes, you can ROLLBACK the transaction, which undoes all changes made since the transaction began, restoring the database to its original state. While a deep dive into transactions is beyond this introduction, understanding their existence is crucial for building robust and reliable database applications..",
      "narration_script": "Finally, let's briefly touch upon a powerful concept for ensuring data integrity: **transactions**. Imagine you're transferring money from one bank account to another. This isn't one single action; it involves deducting money from the first account *and* adding it to the second. What if the deduction succeeds but the addition fails? You'd lose money! A transaction treats a sequence of operations like these as a single, indivisible unit of work. This 'all or nothing' property is called atomicity. In SQL, you can explicitly start a transaction using BEGIN TRANSACTION or START TRANSACTION. You then perform your INSERT, UPDATE, or DELETE statements. If all operations succeed and you're happy with the changes, you COMMIT them, making them permanent in the database. But if anything goes wrong, or you decide you don't want the changes, you can ROLLBACK the transaction, which undoes all changes made since the transaction began, restoring the database to its original state. While a deep dive into transactions is beyond this introduction, understanding their existence is crucial for building robust and reliable database applications.",
      "estimated_duration_seconds": 70,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Finally, let's briefly touch upon a powerful concept for ensuring data integrity: **transactions**.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 1,
          "text": "Imagine you're transferring money from one bank account to another.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 2,
          "text": "This isn't one single action; it involves deducting money from the first account *and* adding it to the second.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 3,
          "text": "What if the deduction succeeds but the addition fails? You'd lose money!",
          "estimated_duration_seconds": 7
        },
        {
          "index": 4,
          "text": "A transaction treats a sequence of operations like these as a single, indivisible unit of work.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 5,
          "text": "This 'all or nothing' property is called atomicity.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 6,
          "text": "In SQL, you can explicitly start a transaction using BEGIN TRANSACTION or START TRANSACTION.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 7,
          "text": "You then perform your INSERT, UPDATE, or DELETE statements.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 8,
          "text": "If all operations succeed and you're happy with the changes, you COMMIT them, making them permanent in the database.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 9,
          "text": "But if anything goes wrong, or you decide you don't want the changes, you can ROLLBACK the transaction, which undoes all changes made since the transaction began, restoring the database to its original state.",
          "estimated_duration_seconds": 18
        },
        {
          "index": 10,
          "text": "While a deep dive into transactions is beyond this introduction, understanding their existence is crucial for building robust and reliable database applications.",
          "estimated_duration_seconds": 15
        }
      ]
    },
    {
      "screen": 7,
      "type": "hands-on",
      "title": "Hands-on: Adding and Updating Inventory",
      "content": "You have an `Inventory` table with the following structure and initial data: | ItemID | ItemName | Quantity | LastUpdated | | :----- | :---------- | :------- | :---------- | | 1 | Widget A | 150 | 2023-01-10 | | 2 | Gadget B | 75 | 2023-01-12 | | 3 | Gizmo C | 200 | 2023-01-11 | Assume `ItemID` is the primary key. Perform the following operations using SQL DML commands: 1. **Insert:** Add a new item: `ItemID` 4, `ItemName` 'Doodad D', `Quantity` 100, `LastUpdated` (today's date, e.g., '2023-10-26'). 2. **Update:** Item 'Widget A' (ItemID 1) had a new shipment. Increase its `Quantity` by 50 and update `LastUpdated` to today's date. Starter Code/Template: `-- 1. Insert a new item INSERT INTO Inventory (ItemID, ItemName, Quantity, LastUpdated) VALUES (...); -- 2. Update Widget A's quantity and LastUpdated date UPDATE Inventory SET Quantity = ..., LastUpdated = 'YYYY-MM-DD' -- Replace YYYY-MM-DD with today's date WHERE ItemID = 1;",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** hands-on\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nYou have an `Inventory` table with the following structure and initial data: | ItemID | ItemName | Quantity | LastUpdated | | :----- | :---------- | :------- | :---------- | | 1 | Widget A | 150 | 2023-01-10 | | 2 | Gadget B | 75 | 2023-01-12 | | 3 | Gizmo C | 200 | 2023-01-11 | Assume `ItemID` is the primary key. Perform the following operations using SQL DML commands: 1. **Insert:** Add a new item: `ItemID` 4, `ItemName` 'Doodad D', `Quantity` 100, `LastUpdated` (today's date, e.g., '2023-10-26'). 2. **Update:** Item 'Widget A' (ItemID 1) had a new shipment. Increase its `Quantity` by 50 and update `LastUpdated` to today's date. Starter Code/Template: `-- 1. Insert a new item INSERT INTO Inventory (ItemID, ItemName, Quantity, LastUpdated) VALUES (...); -- 2. Update Widget A's quantity and LastUpdated date UPDATE Inventory SET Quantity = ..., LastUpdated = 'YYYY-MM-DD' -- Replace YYYY-MM-DD with today's date WHERE ItemID = 1;\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: You have an `Inventory` table with the following structure and initial data: | ItemID | ItemName | Quantity | LastUpdated | | :----- | :---------- | :------- | :---------- | | 1 | Widget A | 150 | 2023-01-10 | | 2 | Gadget B | 75 | 2023-01-12 | | 3 | Gizmo C | 200 | 2023-01-11 | Assume `ItemID` is the primary key. Perform the following operations using SQL DML commands: 1. **Insert:** Add a new item: `ItemID` 4, `ItemName` 'Doodad D', `Quantity` 100, `LastUpdated` (today's date, e.g., '2023-10-26'). 2. **Update:** Item 'Widget A' (ItemID 1) had a new shipment. Increase its `Quantity` by 50 and update `LastUpdated` to today's date. Starter Code/Template: `-- 1. Insert a new item INSERT INTO Inventory (ItemID, ItemName, Quantity, LastUpdated) VALUES (...); -- 2. Update Widget A's quantity and LastUpdated date UPDATE Inventory SET Quantity = ..., LastUpdated = 'YYYY-MM-DD' -- Replace YYYY-MM-DD with today's date WHERE ItemID = 1;. Narration focus: Now it's time to put your knowledge of INSERT and UPDATE into practice with a hands-on activity. We'll be managing an Inventory table, which currently holds information about various items, their quantities, and when they were last updated. Your task for this first part is to perform two operations. First, you'll INSERT a new item into the table: a 'Doodad D' with an ItemID of 4, a Quantity of 100, and LastUpdated to today's date. Remember to explicitly list your columns for good practice. Second, you'll UPDATE an existing item. 'Widget A', with ItemID 1, had a new shipment. You need to increase its Quantity by 50 and also update its LastUpdated date to today. Pay close attention to your WHERE clause to ensure you only update 'Widget A'. Take your time, refer back to the syntax we just covered, and make sure your queries are precise. You'll find starter code provided to guide you..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: Now it's time to put your knowledge of INSERT and UPDATE into practice with a hands-on activity. We'll be managing an Inventory table, which currently holds information about various items, their quantities, and when they were last updated. Your task for this first part is to perform two operations. First, you'll INSERT a new item into the table: a 'Doodad D' with an ItemID of 4, a Quantity of 100, and LastUpdated to today's date. Remember to explicitly list your columns for good practice. Second, you'll UPDATE an existing item. 'Widget A', with ItemID 1, had a new shipment. You need to increase its Quantity by 50 and also update its LastUpdated date to today. Pay close attention to your WHERE clause to ensure you only update 'Widget A'. Take your time, refer back to the syntax we just covered, and make sure your queries are precise. You'll find starter code provided to guide you..",
      "narration_script": "Now it's time to put your knowledge of INSERT and UPDATE into practice with a hands-on activity. We'll be managing an Inventory table, which currently holds information about various items, their quantities, and when they were last updated. Your task for this first part is to perform two operations. First, you'll INSERT a new item into the table: a 'Doodad D' with an ItemID of 4, a Quantity of 100, and LastUpdated to today's date. Remember to explicitly list your columns for good practice. Second, you'll UPDATE an existing item. 'Widget A', with ItemID 1, had a new shipment. You need to increase its Quantity by 50 and also update its LastUpdated date to today. Pay close attention to your WHERE clause to ensure you only update 'Widget A'. Take your time, refer back to the syntax we just covered, and make sure your queries are precise. You'll find starter code provided to guide you.",
      "estimated_duration_seconds": 75,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Now it's time to put your knowledge of INSERT and UPDATE into practice with a hands-on activity.",
          "estimated_duration_seconds": 11
        },
        {
          "index": 1,
          "text": "We'll be managing an Inventory table, which currently holds information about various items, their quantities, and when they were last updated.",
          "estimated_duration_seconds": 13
        },
        {
          "index": 2,
          "text": "Your task for this first part is to perform two operations.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 3,
          "text": "First, you'll INSERT a new item into the table: a 'Doodad D' with an ItemID of 4, a Quantity of 100, and LastUpdated to today's date.",
          "estimated_duration_seconds": 15
        },
        {
          "index": 4,
          "text": "Remember to explicitly list your columns for good practice.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 5,
          "text": "Second, you'll UPDATE an existing item.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 6,
          "text": "'Widget A', with ItemID 1, had a new shipment.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 7,
          "text": "You need to increase its Quantity by 50 and also update its LastUpdated date to today.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 8,
          "text": "Pay close attention to your WHERE clause to ensure you only update 'Widget A'.",
          "estimated_duration_seconds": 9
        },
        {
          "index": 9,
          "text": "Take your time, refer back to the syntax we just covered, and make sure your queries are precise.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 10,
          "text": "You'll find starter code provided to guide you.",
          "estimated_duration_seconds": 5
        }
      ]
    },
    {
      "screen": 8,
      "type": "hands-on",
      "title": "Hands-on: Deleting and Verifying Inventory",
      "content": "Continuing the `Inventory` table activity. Perform the following operations using SQL DML commands: 3. **Update:** Item 'Gadget B' (ItemID 2) was mislabeled. Change its `ItemName` to 'Super Gadget B'. 4. **Delete:** Item 'Gizmo C' (ItemID 3) is being discontinued. Remove it from the inventory. 5. **Bonus (Safety Check):** Write a `SELECT` query that would show you which item(s) would be affected if you ran `DELETE FROM Inventory WHERE Quantity < 100;`. Starter Code/Template: `-- 3. Update Gadget B's ItemName UPDATE Inventory SET ItemName = ... WHERE ItemID = 2; -- 4. Delete Gizmo C DELETE FROM Inventory WHERE ...; -- 5. Bonus: Safety check for deleting items with quantity < 100 SELECT * FROM Inventory WHERE ...;",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Data Manipulation: INSERT, UPDATE, DELETE\n**Screen Type:** hands-on\n\n**Learning Objectives:**\n*   Write `INSERT` statements to add new rows of data into a table, specifying values for all or specific columns.\n*   Construct `UPDATE` statements to modify existing data in one or more rows, using the `WHERE` clause to target specific records.\n*   Formulate `DELETE` statements to remove rows from a table, understanding the critical importance of the `WHERE` clause.\n*   Explain the potential dangers of `UPDATE` and `DELETE` commands without a `WHERE` clause and how to prevent accidental data loss.\n*   Understand the basic concept of a transaction in the context of data modification.\n\n**Lesson Content:**\nContinuing the `Inventory` table activity. Perform the following operations using SQL DML commands: 3. **Update:** Item 'Gadget B' (ItemID 2) was mislabeled. Change its `ItemName` to 'Super Gadget B'. 4. **Delete:** Item 'Gizmo C' (ItemID 3) is being discontinued. Remove it from the inventory. 5. **Bonus (Safety Check):** Write a `SELECT` query that would show you which item(s) would be affected if you ran `DELETE FROM Inventory WHERE Quantity < 100;`. Starter Code/Template: `-- 3. Update Gadget B's ItemName UPDATE Inventory SET ItemName = ... WHERE ItemID = 2; -- 4. Delete Gizmo C DELETE FROM Inventory WHERE ...; -- 5. Bonus: Safety check for deleting items with quantity < 100 SELECT * FROM Inventory WHERE ...;\n\n**Key Concepts:**\n*   **Data Manipulation Language (DML):** A subset of SQL commands used for adding, modifying, and deleting data in a database (e.g., `INSERT`, `UPDATE`, `DELETE`).\n*   **`INSERT` statement:** Used to add new rows (records) of data into a table.\n*   **`UPDATE` statement:** Used to modify existing data in one or more rows of a table.\n*   **`SET` clause:** Used within an `UPDATE` statement to specify which columns to change and their new values.\n*   **`DELETE` statement:** Used to remove one or more rows from a table.\n*   **`WHERE` clause (DML context):** Crucial for `UPDATE` and `DELETE` statements to specify which specific rows should be affected, preventing accidental modification or deletion of all data.\n*   **Transaction:** A single logical unit of work, comprising one or more database operations, that is treated as a whole (either all succeed or all fail).\n*   **`COMMIT`:** Makes all changes within a transaction permanent.\n*   **`ROLLBACK`:** Undoes all changes within a transaction, restoring the database to its state before the transaction began.\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.3: SQL Data Manipulation: INSERT, UPDATE, DELETE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: Continuing the `Inventory` table activity. Perform the following operations using SQL DML commands: 3. **Update:** Item 'Gadget B' (ItemID 2) was mislabeled. Change its `ItemName` to 'Super Gadget B'. 4. **Delete:** Item 'Gizmo C' (ItemID 3) is being discontinued. Remove it from the inventory. 5. **Bonus (Safety Check):** Write a `SELECT` query that would show you which item(s) would be affected if you ran `DELETE FROM Inventory WHERE Quantity < 100;`. Starter Code/Template: `-- 3. Update Gadget B's ItemName UPDATE Inventory SET ItemName = ... WHERE ItemID = 2; -- 4. Delete Gizmo C DELETE FROM Inventory WHERE ...; -- 5. Bonus: Safety check for deleting items with quantity < 100 SELECT * FROM Inventory WHERE ...;. Narration focus: Continuing our hands-on activity with the Inventory table, your next tasks involve further updates, deletion, and critically, a safety check. First, you need to UPDATE 'Gadget B', with ItemID 2, because it was mislabeled. Change its ItemName to 'Super Gadget B'. Remember your WHERE clause! Next, 'Gizmo C', ItemID 3, is being discontinued. You'll need to DELETE this item from the inventory. Again, ensure your WHERE clause precisely targets only 'Gizmo C'. Finally, for a crucial bonus safety check: write a SELECT query that would show you *which item or items* would be affected if you were to run a DELETE FROM Inventory WHERE Quantity < 100; This practice reinforces the importance of verifying your WHERE conditions before executing potentially destructive commands. Complete these steps carefully, using the provided starter code as a guide..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Data Manipulation: INSERT, UPDATE, DELETE. The visual should reflect the narration tone and explanation flow: Continuing our hands-on activity with the Inventory table, your next tasks involve further updates, deletion, and critically, a safety check. First, you need to UPDATE 'Gadget B', with ItemID 2, because it was mislabeled. Change its ItemName to 'Super Gadget B'. Remember your WHERE clause! Next, 'Gizmo C', ItemID 3, is being discontinued. You'll need to DELETE this item from the inventory. Again, ensure your WHERE clause precisely targets only 'Gizmo C'. Finally, for a crucial bonus safety check: write a SELECT query that would show you *which item or items* would be affected if you were to run a DELETE FROM Inventory WHERE Quantity < 100; This practice reinforces the importance of verifying your WHERE conditions before executing potentially destructive commands. Complete these steps carefully, using the provided starter code as a guide..",
      "narration_script": "Continuing our hands-on activity with the Inventory table, your next tasks involve further updates, deletion, and critically, a safety check. First, you need to UPDATE 'Gadget B', with ItemID 2, because it was mislabeled. Change its ItemName to 'Super Gadget B'. Remember your WHERE clause! Next, 'Gizmo C', ItemID 3, is being discontinued. You'll need to DELETE this item from the inventory. Again, ensure your WHERE clause precisely targets only 'Gizmo C'. Finally, for a crucial bonus safety check: write a SELECT query that would show you *which item or items* would be affected if you were to run a DELETE FROM Inventory WHERE Quantity < 100; This practice reinforces the importance of verifying your WHERE conditions before executing potentially destructive commands. Complete these steps carefully, using the provided starter code as a guide.",
      "estimated_duration_seconds": 75,
      "speaking_style": "default",
      "delivery_notes": [],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Continuing our hands-on activity with the Inventory table, your next tasks involve further updates, deletion, and critically, a safety check.",
          "estimated_duration_seconds": 14
        },
        {
          "index": 1,
          "text": "First, you need to UPDATE 'Gadget B', with ItemID 2, because it was mislabeled.",
          "estimated_duration_seconds": 9
        },
        {
          "index": 2,
          "text": "Change its ItemName to 'Super Gadget B'.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 3,
          "text": "Remember your WHERE clause!",
          "estimated_duration_seconds": 3
        },
        {
          "index": 4,
          "text": "Next, 'Gizmo C', ItemID 3, is being discontinued.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 5,
          "text": "You'll need to DELETE this item from the inventory.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 6,
          "text": "Again, ensure your WHERE clause precisely targets only 'Gizmo C'.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 7,
          "text": "Finally, for a crucial bonus safety check: write a SELECT query that would show you *which item or items* would be affected if you were to run a DELETE FROM Inventory WHERE Quantity < 100;",
          "estimated_duration_seconds": 18
        },
        {
          "index": 8,
          "text": "This practice reinforces the importance of verifying your WHERE conditions before executing potentially destructive commands.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 9,
          "text": "Complete these steps carefully, using the provided starter code as a guide.",
          "estimated_duration_seconds": 9
        }
      ]
    }
  ]
}