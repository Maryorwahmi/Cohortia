{
  "scope": "chapter",
  "title": "5.2: SQL Fundamentals: SELECT, FROM, WHERE",
  "summary": "This chapter introduces SQL as the standard language for interacting with relational databases, focusing on the fundamental SELECT, FROM, and WHERE clauses. Learners will master how to retrieve specific data by selecting columns, specifying target tables, and filtering rows using various comparison and logical operators.",
  "estimated_total_duration_seconds": 380,
  "screens": [
    {
      "screen": 1,
      "type": "intro",
      "title": "Welcome to SQL Fundamentals",
      "content": "Now that we understand the structure of a relational database, it's time to learn how to communicate with it. The standard language for interacting with relational databases is SQL, which stands for Structured Query Language. SQL is a declarative language specifically designed for managing and manipulating data in RDBMS. This means you tell the database what you want to achieve, rather than how to achieve it. Our focus in this chapter is on the most fundamental and frequently used SQL command: SELECT, which allows us to retrieve data.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** intro\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nNow that we understand the structure of a relational database, it's time to learn how to communicate with it. The standard language for interacting with relational databases is SQL, which stands for Structured Query Language. SQL is a declarative language specifically designed for managing and manipulating data in RDBMS. This means you tell the database what you want to achieve, rather than how to achieve it. Our focus in this chapter is on the most fundamental and frequently used SQL command: SELECT, which allows us to retrieve data.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: Now that we understand the structure of a relational database, it's time to learn how to communicate with it. The standard language for interacting with relational databases is SQL, which stands for Structured Query Language. SQL is a declarative language specifically designed for managing and manipulating data in RDBMS. This means you tell the database what you want to achieve, rather than how to achieve it. Our focus in this chapter is on the most fundamental and frequently used SQL command: SELECT, which allows us to retrieve data.. Narration focus: Welcome to our deep dive into SQL Fundamentals. In this chapter, we're going to unlock the power of databases by learning how to communicate with them using Structured Query Language, or SQL. SQL isn't a general-purpose programming language like Python; it's a specialized, declarative language. This means you tell the database *what* you want, and it figures out *how* to get it. Our primary focus will be on the most essential SQL command: `SELECT`, which is all about retrieving data. By the end of this lesson, you'll be able to construct basic `SELECT` statements, specify which tables to pull from using `FROM`, and filter your results precisely with the `WHERE` clause. You'll also learn the difference between grabbing all columns versus just a few, and effectively use comparison and logical operators to fine-tune your queries..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: Welcome to our deep dive into SQL Fundamentals. In this chapter, we're going to unlock the power of databases by learning how to communicate with them using Structured Query Language, or SQL. SQL isn't a general-purpose programming language like Python; it's a specialized, declarative language. This means you tell the database *what* you want, and it figures out *how* to get it. Our primary focus will be on the most essential SQL command: `SELECT`, which is all about retrieving data. By the end of this lesson, you'll be able to construct basic `SELECT` statements, specify which tables to pull from using `FROM`, and filter your results precisely with the `WHERE` clause. You'll also learn the difference between grabbing all columns versus just a few, and effectively use comparison and logical operators to fine-tune your queries..",
      "narration_script": "Welcome to our deep dive into SQL Fundamentals. In this chapter, we're going to unlock the power of databases by learning how to communicate with them using Structured Query Language, or SQL. SQL isn't a general-purpose programming language like Python; it's a specialized, declarative language. This means you tell the database *what* you want, and it figures out *how* to get it. Our primary focus will be on the most essential SQL command: `SELECT`, which is all about retrieving data. By the end of this lesson, you'll be able to construct basic `SELECT` statements, specify which tables to pull from using `FROM`, and filter your results precisely with the `WHERE` clause. You'll also learn the difference between grabbing all columns versus just a few, and effectively use comparison and logical operators to fine-tune your queries.",
      "estimated_duration_seconds": 45,
      "speaking_style": "default",
      "delivery_notes": [
        "Emphasize 'what' vs 'how' for declarative language.",
        "Brief pause before listing objectives."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Welcome to our deep dive into SQL Fundamentals.",
          "estimated_duration_seconds": 3
        },
        {
          "index": 1,
          "text": "In this chapter, we're going to unlock the power of databases by learning how to communicate with them using Structured Query Language, or SQL.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 2,
          "text": "SQL isn't a general-purpose programming language like Python; it's a specialized, declarative language.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 3,
          "text": "This means you tell the database *what* you want, and it figures out *how* to get it.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 4,
          "text": "Our primary focus will be on the most essential SQL command: `SELECT`, which is all about retrieving data.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 5,
          "text": "By the end of this lesson, you'll be able to construct basic `SELECT` statements, specify which tables to pull from using `FROM`, and filter your results precisely with the `WHERE` clause.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 6,
          "text": "You'll also learn the difference between grabbing all columns versus just a few, and effectively use comparison and logical operators to fine-tune your queries.",
          "estimated_duration_seconds": 7
        }
      ]
    },
    {
      "screen": 2,
      "type": "concept",
      "title": "The Core of Querying: SELECT and FROM",
      "content": "The `SELECT` statement is the core of querying data. At its simplest, it allows you to pick which columns you want to see from a table. The basic syntax involves two main clauses: `SELECT` and `FROM`. The `SELECT` clause specifies the columns you wish to retrieve, and the `FROM` clause indicates which table contains those columns. To retrieve all columns for all students from a `Students` table, you can use the asterisk (`*`) wildcard. However, for efficiency and readability, you often select specific columns by listing their names.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** concept\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nThe `SELECT` statement is the core of querying data. At its simplest, it allows you to pick which columns you want to see from a table. The basic syntax involves two main clauses: `SELECT` and `FROM`. The `SELECT` clause specifies the columns you wish to retrieve, and the `FROM` clause indicates which table contains those columns. To retrieve all columns for all students from a `Students` table, you can use the asterisk (`*`) wildcard. However, for efficiency and readability, you often select specific columns by listing their names.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: The `SELECT` statement is the core of querying data. At its simplest, it allows you to pick which columns you want to see from a table. The basic syntax involves two main clauses: `SELECT` and `FROM`. The `SELECT` clause specifies the columns you wish to retrieve, and the `FROM` clause indicates which table contains those columns. To retrieve all columns for all students from a `Students` table, you can use the asterisk (`*`) wildcard. However, for efficiency and readability, you often select specific columns by listing their names.. Narration focus: The `SELECT` statement is truly the heart of data querying. It's how you tell the database exactly which pieces of information you want to retrieve. The most basic `SELECT` query always involves two key clauses: `SELECT` to specify *what* columns you want, and `FROM` to specify *where* – meaning, which table – those columns reside. Imagine we have a `Students` table. To get every single piece of data for all students, you can use the asterisk, or `*`, as a wildcard. So, `SELECT * FROM Students;` would fetch all columns and all rows. This is handy for a quick look. But often, you only need specific information. To retrieve just the `FirstName` and `LastName`, you simply list them, separated by commas, like `SELECT FirstName, LastName FROM Students;`. This is more efficient and keeps your results focused, especially with large tables..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: The `SELECT` statement is truly the heart of data querying. It's how you tell the database exactly which pieces of information you want to retrieve. The most basic `SELECT` query always involves two key clauses: `SELECT` to specify *what* columns you want, and `FROM` to specify *where* – meaning, which table – those columns reside. Imagine we have a `Students` table. To get every single piece of data for all students, you can use the asterisk, or `*`, as a wildcard. So, `SELECT * FROM Students;` would fetch all columns and all rows. This is handy for a quick look. But often, you only need specific information. To retrieve just the `FirstName` and `LastName`, you simply list them, separated by commas, like `SELECT FirstName, LastName FROM Students;`. This is more efficient and keeps your results focused, especially with large tables..",
      "narration_script": "The `SELECT` statement is truly the heart of data querying. It's how you tell the database exactly which pieces of information you want to retrieve. The most basic `SELECT` query always involves two key clauses: `SELECT` to specify *what* columns you want, and `FROM` to specify *where* – meaning, which table – those columns reside. Imagine we have a `Students` table. To get every single piece of data for all students, you can use the asterisk, or `*`, as a wildcard. So, `SELECT * FROM Students;` would fetch all columns and all rows. This is handy for a quick look. But often, you only need specific information. To retrieve just the `FirstName` and `LastName`, you simply list them, separated by commas, like `SELECT FirstName, LastName FROM Students;`. This is more efficient and keeps your results focused, especially with large tables.",
      "estimated_duration_seconds": 55,
      "speaking_style": "default",
      "delivery_notes": [
        "Explain `*` clearly.",
        "Emphasize efficiency of specific column selection."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "The `SELECT` statement is truly the heart of data querying.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 1,
          "text": "It's how you tell the database exactly which pieces of information you want to retrieve.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 2,
          "text": "The most basic `SELECT` query always involves two key clauses: `SELECT` to specify *what* columns you want, and `FROM` to specify *where* – meaning, which table – those columns reside.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 3,
          "text": "Imagine we have a `Students` table.",
          "estimated_duration_seconds": 2
        },
        {
          "index": 4,
          "text": "To get every single piece of data for all students, you can use the asterisk, or `*`, as a wildcard.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 5,
          "text": "So, `SELECT * FROM Students;` would fetch all columns and all rows.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 6,
          "text": "This is handy for a quick look.",
          "estimated_duration_seconds": 2
        },
        {
          "index": 7,
          "text": "But often, you only need specific information.",
          "estimated_duration_seconds": 3
        },
        {
          "index": 8,
          "text": "To retrieve just the `FirstName` and `LastName`, you simply list them, separated by commas, like `SELECT FirstName, LastName FROM Students;`.",
          "estimated_duration_seconds": 9
        },
        {
          "index": 9,
          "text": "This is more efficient and keeps your results focused, especially with large tables.",
          "estimated_duration_seconds": 5
        }
      ]
    },
    {
      "screen": 3,
      "type": "concept",
      "title": "Filtering Data with WHERE",
      "content": "While `SELECT` and `FROM` determine which columns to retrieve and from where, the `WHERE` clause is crucial for *which* rows to retrieve. The `WHERE` clause allows you to filter the data based on specific conditions, returning only the rows that satisfy those conditions. It comes after the `FROM` clause and uses comparison operators to define conditions. Common operators include `=`, `!=` (or `<>`), `>`, `<`, `>=`, and `<=`. String values require single quotes, while numeric values do not.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** concept\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nWhile `SELECT` and `FROM` determine which columns to retrieve and from where, the `WHERE` clause is crucial for *which* rows to retrieve. The `WHERE` clause allows you to filter the data based on specific conditions, returning only the rows that satisfy those conditions. It comes after the `FROM` clause and uses comparison operators to define conditions. Common operators include `=`, `!=` (or `<>`), `>`, `<`, `>=`, and `<=`. String values require single quotes, while numeric values do not.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: While `SELECT` and `FROM` determine which columns to retrieve and from where, the `WHERE` clause is crucial for *which* rows to retrieve. The `WHERE` clause allows you to filter the data based on specific conditions, returning only the rows that satisfy those conditions. It comes after the `FROM` clause and uses comparison operators to define conditions. Common operators include `=`, `!=` (or `<>`), `>`, `<`, `>=`, and `<=`. String values require single quotes, while numeric values do not.. Narration focus: Now, while `SELECT` and `FROM` tell us *what* columns to get and *from where*, the `WHERE` clause is where the real power of filtering comes in. It's how you specify *which* rows you want to retrieve. The `WHERE` clause always comes after your `FROM` clause and uses comparison operators to set conditions. Think of it like a gatekeeper, only letting through the data that meets your criteria. We have operators like equals (`=`), not equals (`!=` or `<>`), greater than (`>`), less than (`<`), and their 'or equal to' counterparts (`>=`, `<=`). For example, to find all students majoring in 'Computer Science', you'd write `WHERE Major = 'Computer Science'`. Notice the single quotes around 'Computer Science' because it's a string. If you were looking for students with a GPA greater than or equal to 3.5, you'd use `WHERE GPA >= 3.5`, without quotes around the number..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: Now, while `SELECT` and `FROM` tell us *what* columns to get and *from where*, the `WHERE` clause is where the real power of filtering comes in. It's how you specify *which* rows you want to retrieve. The `WHERE` clause always comes after your `FROM` clause and uses comparison operators to set conditions. Think of it like a gatekeeper, only letting through the data that meets your criteria. We have operators like equals (`=`), not equals (`!=` or `<>`), greater than (`>`), less than (`<`), and their 'or equal to' counterparts (`>=`, `<=`). For example, to find all students majoring in 'Computer Science', you'd write `WHERE Major = 'Computer Science'`. Notice the single quotes around 'Computer Science' because it's a string. If you were looking for students with a GPA greater than or equal to 3.5, you'd use `WHERE GPA >= 3.5`, without quotes around the number..",
      "narration_script": "Now, while `SELECT` and `FROM` tell us *what* columns to get and *from where*, the `WHERE` clause is where the real power of filtering comes in. It's how you specify *which* rows you want to retrieve. The `WHERE` clause always comes after your `FROM` clause and uses comparison operators to set conditions. Think of it like a gatekeeper, only letting through the data that meets your criteria. We have operators like equals (`=`), not equals (`!=` or `<>`), greater than (`>`), less than (`<`), and their 'or equal to' counterparts (`>=`, `<=`). For example, to find all students majoring in 'Computer Science', you'd write `WHERE Major = 'Computer Science'`. Notice the single quotes around 'Computer Science' because it's a string. If you were looking for students with a GPA greater than or equal to 3.5, you'd use `WHERE GPA >= 3.5`, without quotes around the number.",
      "estimated_duration_seconds": 60,
      "speaking_style": "default",
      "delivery_notes": [
        "Analogy for WHERE clause.",
        "Clearly differentiate string vs. numeric handling."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Now, while `SELECT` and `FROM` tell us *what* columns to get and *from where*, the `WHERE` clause is where the real power of filtering comes in.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 1,
          "text": "It's how you specify *which* rows you want to retrieve.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 2,
          "text": "The `WHERE` clause always comes after your `FROM` clause and uses comparison operators to set conditions.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 3,
          "text": "Think of it like a gatekeeper, only letting through the data that meets your criteria.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 4,
          "text": "We have operators like equals (`=`), not equals (`!=` or `<>`), greater than (`>`), less than (`<`), and their 'or equal to' counterparts (`>=`, `<=`).",
          "estimated_duration_seconds": 10
        },
        {
          "index": 5,
          "text": "For example, to find all students majoring in 'Computer Science', you'd write `WHERE Major = 'Computer Science'`.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 6,
          "text": "Notice the single quotes around 'Computer Science' because it's a string.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 7,
          "text": "If you were looking for students with a GPA greater than or equal to 3.5, you'd use `WHERE GPA >= 3.5`, without quotes around the number.",
          "estimated_duration_seconds": 7
        }
      ]
    },
    {
      "screen": 4,
      "type": "concept",
      "title": "Advanced Filtering: AND, OR, NOT",
      "content": "You can combine multiple conditions using **logical operators**: `AND`, `OR`, and `NOT`. `AND` requires both conditions to be true, `OR` requires at least one to be true, and `NOT` negates a condition. It's crucial to understand operator precedence: `NOT` is evaluated first, then `AND`, then `OR`. Always use parentheses to explicitly control the order of evaluation, especially when mixing `AND` and `OR`, to ensure clarity and correctness. A common mistake with `OR` is not repeating the column for each condition.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** concept\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nYou can combine multiple conditions using **logical operators**: `AND`, `OR`, and `NOT`. `AND` requires both conditions to be true, `OR` requires at least one to be true, and `NOT` negates a condition. It's crucial to understand operator precedence: `NOT` is evaluated first, then `AND`, then `OR`. Always use parentheses to explicitly control the order of evaluation, especially when mixing `AND` and `OR`, to ensure clarity and correctness. A common mistake with `OR` is not repeating the column for each condition.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: You can combine multiple conditions using **logical operators**: `AND`, `OR`, and `NOT`. `AND` requires both conditions to be true, `OR` requires at least one to be true, and `NOT` negates a condition. It's crucial to understand operator precedence: `NOT` is evaluated first, then `AND`, then `OR`. Always use parentheses to explicitly control the order of evaluation, especially when mixing `AND` and `OR`, to ensure clarity and correctness. A common mistake with `OR` is not repeating the column for each condition.. Narration focus: To build more sophisticated filters, we use **logical operators**: `AND`, `OR`, and `NOT`. `AND` is straightforward: *both* conditions must be true for a row to be included. For example, `WHERE Major = 'CS' AND GPA > 3.5` would find only CS majors *with* a high GPA. `OR` is more inclusive: *at least one* of the conditions must be true. So, `WHERE Major = 'CS' OR Major = 'Mathematics'` would give you students from either department. A common pitfall here is trying to shorten it to `OR 'Mathematics'`; this is incorrect, you must specify the column for each condition. Finally, `NOT` inverts a condition. `WHERE NOT Major = 'CS'` would find everyone *except* Computer Science majors. Remember the order of operations: `NOT` first, then `AND`, then `OR`. When in doubt, use parentheses to explicitly group your conditions and guarantee your logic is evaluated exactly as you intend..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: To build more sophisticated filters, we use **logical operators**: `AND`, `OR`, and `NOT`. `AND` is straightforward: *both* conditions must be true for a row to be included. For example, `WHERE Major = 'CS' AND GPA > 3.5` would find only CS majors *with* a high GPA. `OR` is more inclusive: *at least one* of the conditions must be true. So, `WHERE Major = 'CS' OR Major = 'Mathematics'` would give you students from either department. A common pitfall here is trying to shorten it to `OR 'Mathematics'`; this is incorrect, you must specify the column for each condition. Finally, `NOT` inverts a condition. `WHERE NOT Major = 'CS'` would find everyone *except* Computer Science majors. Remember the order of operations: `NOT` first, then `AND`, then `OR`. When in doubt, use parentheses to explicitly group your conditions and guarantee your logic is evaluated exactly as you intend..",
      "narration_script": "To build more sophisticated filters, we use **logical operators**: `AND`, `OR`, and `NOT`. `AND` is straightforward: *both* conditions must be true for a row to be included. For example, `WHERE Major = 'CS' AND GPA > 3.5` would find only CS majors *with* a high GPA. `OR` is more inclusive: *at least one* of the conditions must be true. So, `WHERE Major = 'CS' OR Major = 'Mathematics'` would give you students from either department. A common pitfall here is trying to shorten it to `OR 'Mathematics'`; this is incorrect, you must specify the column for each condition. Finally, `NOT` inverts a condition. `WHERE NOT Major = 'CS'` would find everyone *except* Computer Science majors. Remember the order of operations: `NOT` first, then `AND`, then `OR`. When in doubt, use parentheses to explicitly group your conditions and guarantee your logic is evaluated exactly as you intend.",
      "estimated_duration_seconds": 65,
      "speaking_style": "default",
      "delivery_notes": [
        "Clearly explain each operator.",
        "Emphasize the 'OR' common mistake.",
        "Stress the importance of parentheses."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "To build more sophisticated filters, we use **logical operators**: `AND`, `OR`, and `NOT`.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 1,
          "text": "`AND` is straightforward: *both* conditions must be true for a row to be included.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 2,
          "text": "For example, `WHERE Major = 'CS' AND GPA > 3.5` would find only CS majors *with* a high GPA.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 3,
          "text": "`OR` is more inclusive: *at least one* of the conditions must be true.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 4,
          "text": "So, `WHERE Major = 'CS' OR Major = 'Mathematics'` would give you students from either department.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 5,
          "text": "A common pitfall here is trying to shorten it to `OR 'Mathematics'`; this is incorrect, you must specify the column for each condition.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 6,
          "text": "Finally, `NOT` inverts a condition.",
          "estimated_duration_seconds": 3
        },
        {
          "index": 7,
          "text": "`WHERE NOT Major = 'CS'` would find everyone *except* Computer Science majors.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 8,
          "text": "Remember the order of operations: `NOT` first, then `AND`, then `OR`.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 9,
          "text": "When in doubt, use parentheses to explicitly group your conditions and guarantee your logic is evaluated exactly as you intend.",
          "estimated_duration_seconds": 7
        }
      ]
    },
    {
      "screen": 5,
      "type": "diagram",
      "title": "Deconstructing a SQL Query",
      "content": "A basic SQL `SELECT` query follows a specific structure: `SELECT` specifies the desired columns, `FROM` identifies the source table, and `WHERE` filters the rows. This flow ensures a logical and efficient retrieval of data. Data conceptually moves from the entire table, through the `WHERE` clause for row filtering, and then through the `SELECT` clause for column projection, resulting in a refined dataset.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** diagram\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nA basic SQL `SELECT` query follows a specific structure: `SELECT` specifies the desired columns, `FROM` identifies the source table, and `WHERE` filters the rows. This flow ensures a logical and efficient retrieval of data. Data conceptually moves from the entire table, through the `WHERE` clause for row filtering, and then through the `SELECT` clause for column projection, resulting in a refined dataset.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: A basic SQL `SELECT` query follows a specific structure: `SELECT` specifies the desired columns, `FROM` identifies the source table, and `WHERE` filters the rows. This flow ensures a logical and efficient retrieval of data. Data conceptually moves from the entire table, through the `WHERE` clause for row filtering, and then through the `SELECT` clause for column projection, resulting in a refined dataset.. Narration focus: Let's visualize how a basic SQL query is processed. Think of it as a pipeline for your data. First, the `FROM` clause kicks things off by identifying the source: your entire table. Imagine a stream of all your raw data. Next, that data flows into the `WHERE` clause. This is your filter stage. Here, conditions are applied, and only the rows that satisfy those conditions are allowed to pass through. Rows that don't meet the criteria are discarded. Finally, the filtered rows move to the `SELECT` clause. This is where you project your desired columns. From the remaining rows, only the specified columns are extracted, creating your final, refined result set. This sequential processing – from table, to filtered rows, to selected columns – is fundamental to understanding SQL query execution..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: Let's visualize how a basic SQL query is processed. Think of it as a pipeline for your data. First, the `FROM` clause kicks things off by identifying the source: your entire table. Imagine a stream of all your raw data. Next, that data flows into the `WHERE` clause. This is your filter stage. Here, conditions are applied, and only the rows that satisfy those conditions are allowed to pass through. Rows that don't meet the criteria are discarded. Finally, the filtered rows move to the `SELECT` clause. This is where you project your desired columns. From the remaining rows, only the specified columns are extracted, creating your final, refined result set. This sequential processing – from table, to filtered rows, to selected columns – is fundamental to understanding SQL query execution..",
      "narration_script": "Let's visualize how a basic SQL query is processed. Think of it as a pipeline for your data. First, the `FROM` clause kicks things off by identifying the source: your entire table. Imagine a stream of all your raw data. Next, that data flows into the `WHERE` clause. This is your filter stage. Here, conditions are applied, and only the rows that satisfy those conditions are allowed to pass through. Rows that don't meet the criteria are discarded. Finally, the filtered rows move to the `SELECT` clause. This is where you project your desired columns. From the remaining rows, only the specified columns are extracted, creating your final, refined result set. This sequential processing – from table, to filtered rows, to selected columns – is fundamental to understanding SQL query execution.",
      "estimated_duration_seconds": 60,
      "speaking_style": "default",
      "delivery_notes": [
        "Use 'pipeline' analogy.",
        "Clearly explain each stage's role and order."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Let's visualize how a basic SQL query is processed.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 1,
          "text": "Think of it as a pipeline for your data.",
          "estimated_duration_seconds": 3
        },
        {
          "index": 2,
          "text": "First, the `FROM` clause kicks things off by identifying the source: your entire table.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 3,
          "text": "Imagine a stream of all your raw data.",
          "estimated_duration_seconds": 3
        },
        {
          "index": 4,
          "text": "Next, that data flows into the `WHERE` clause.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 5,
          "text": "This is your filter stage.",
          "estimated_duration_seconds": 2
        },
        {
          "index": 6,
          "text": "Here, conditions are applied, and only the rows that satisfy those conditions are allowed to pass through.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 7,
          "text": "Rows that don't meet the criteria are discarded.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 8,
          "text": "Finally, the filtered rows move to the `SELECT` clause.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 9,
          "text": "This is where you project your desired columns.",
          "estimated_duration_seconds": 4
        },
        {
          "index": 10,
          "text": "From the remaining rows, only the specified columns are extracted, creating your final, refined result set.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 11,
          "text": "This sequential processing – from table, to filtered rows, to selected columns – is fundamental to understanding SQL query execution.",
          "estimated_duration_seconds": 9
        }
      ]
    },
    {
      "screen": 6,
      "type": "diagram",
      "title": "Visualizing Conditional Logic",
      "content": "Understanding how `AND`, `OR`, and `NOT` work together is key to precise data filtering. `AND` narrows results by requiring all conditions to be true, like an intersection. `OR` broadens results by accepting any true condition, like a union. `NOT` excludes data that meets a condition. Operator precedence dictates that `NOT` is applied first, then `AND`, then `OR`. Using parentheses allows you to override this default order and group conditions for complex logic, ensuring your query executes exactly as intended.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** diagram\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nUnderstanding how `AND`, `OR`, and `NOT` work together is key to precise data filtering. `AND` narrows results by requiring all conditions to be true, like an intersection. `OR` broadens results by accepting any true condition, like a union. `NOT` excludes data that meets a condition. Operator precedence dictates that `NOT` is applied first, then `AND`, then `OR`. Using parentheses allows you to override this default order and group conditions for complex logic, ensuring your query executes exactly as intended.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: Understanding how `AND`, `OR`, and `NOT` work together is key to precise data filtering. `AND` narrows results by requiring all conditions to be true, like an intersection. `OR` broadens results by accepting any true condition, like a union. `NOT` excludes data that meets a condition. Operator precedence dictates that `NOT` is applied first, then `AND`, then `OR`. Using parentheses allows you to override this default order and group conditions for complex logic, ensuring your query executes exactly as intended.. Narration focus: When combining multiple conditions in your `WHERE` clause, understanding logical operators and their precedence is vital. Think of `AND` as an intersection: a record must satisfy *all* linked conditions to be included. If you're looking for students in 'Computer Science' AND with a GPA above 3.5, both must be true. `OR` is like a union: a record is included if it satisfies *any* of the linked conditions. So, students in 'Computer Science' OR 'Mathematics' would include either. `NOT` simply negates a condition, excluding records that match. The database processes these in a specific order: `NOT` first, then `AND`, then `OR`. This is crucial. If you want to override this default order, you must use parentheses, just like in algebra, to explicitly group your conditions. This ensures your complex filtering logic is always interpreted correctly..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: When combining multiple conditions in your `WHERE` clause, understanding logical operators and their precedence is vital. Think of `AND` as an intersection: a record must satisfy *all* linked conditions to be included. If you're looking for students in 'Computer Science' AND with a GPA above 3.5, both must be true. `OR` is like a union: a record is included if it satisfies *any* of the linked conditions. So, students in 'Computer Science' OR 'Mathematics' would include either. `NOT` simply negates a condition, excluding records that match. The database processes these in a specific order: `NOT` first, then `AND`, then `OR`. This is crucial. If you want to override this default order, you must use parentheses, just like in algebra, to explicitly group your conditions. This ensures your complex filtering logic is always interpreted correctly..",
      "narration_script": "When combining multiple conditions in your `WHERE` clause, understanding logical operators and their precedence is vital. Think of `AND` as an intersection: a record must satisfy *all* linked conditions to be included. If you're looking for students in 'Computer Science' AND with a GPA above 3.5, both must be true. `OR` is like a union: a record is included if it satisfies *any* of the linked conditions. So, students in 'Computer Science' OR 'Mathematics' would include either. `NOT` simply negates a condition, excluding records that match. The database processes these in a specific order: `NOT` first, then `AND`, then `OR`. This is crucial. If you want to override this default order, you must use parentheses, just like in algebra, to explicitly group your conditions. This ensures your complex filtering logic is always interpreted correctly.",
      "estimated_duration_seconds": 60,
      "speaking_style": "default",
      "delivery_notes": [
        "Use Venn diagram analogies for AND/OR.",
        "Reiterate precedence and parentheses importance."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "When combining multiple conditions in your `WHERE` clause, understanding logical operators and their precedence is vital.",
          "estimated_duration_seconds": 8
        },
        {
          "index": 1,
          "text": "Think of `AND` as an intersection: a record must satisfy *all* linked conditions to be included.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 2,
          "text": "If you're looking for students in 'Computer Science' AND with a GPA above 3.5, both must be true.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 3,
          "text": "`OR` is like a union: a record is included if it satisfies *any* of the linked conditions.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 4,
          "text": "So, students in 'Computer Science' OR 'Mathematics' would include either.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 5,
          "text": "`NOT` simply negates a condition, excluding records that match.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 6,
          "text": "The database processes these in a specific order: `NOT` first, then `AND`, then `OR`.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 7,
          "text": "This is crucial.",
          "estimated_duration_seconds": 1
        },
        {
          "index": 8,
          "text": "If you want to override this default order, you must use parentheses, just like in algebra, to explicitly group your conditions.",
          "estimated_duration_seconds": 9
        },
        {
          "index": 9,
          "text": "This ensures your complex filtering logic is always interpreted correctly.",
          "estimated_duration_seconds": 4
        }
      ]
    },
    {
      "screen": 7,
      "type": "hands-on",
      "title": "Hands-On: Querying Product Data - Part 1",
      "content": "In this activity, you'll practice using `SELECT` and `FROM` to retrieve data, and the `WHERE` clause with basic comparison operators to filter rows. We'll be working with a simulated `Products` table. Your task is to write SQL queries to select specific columns, all columns, and filter products based on their category and price. Pay close attention to the syntax for string and numeric comparisons.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** hands-on\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nIn this activity, you'll practice using `SELECT` and `FROM` to retrieve data, and the `WHERE` clause with basic comparison operators to filter rows. We'll be working with a simulated `Products` table. Your task is to write SQL queries to select specific columns, all columns, and filter products based on their category and price. Pay close attention to the syntax for string and numeric comparisons.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: In this activity, you'll practice using `SELECT` and `FROM` to retrieve data, and the `WHERE` clause with basic comparison operators to filter rows. We'll be working with a simulated `Products` table. Your task is to write SQL queries to select specific columns, all columns, and filter products based on their category and price. Pay close attention to the syntax for string and numeric comparisons.. Narration focus: Alright, it's time for some hands-on practice! We're going to work with a simulated `Products` table, which has columns like `ProductID`, `ProductName`, `Category`, `Price`, and `StockQuantity`. For this first part of the activity, your goal is to practice retrieving data using `SELECT` and `FROM`, and then filtering it with the `WHERE` clause using basic comparison operators. You'll start by selecting specific columns like `ProductName` and `Price` for all products. Then, you'll retrieve all columns for products belonging to a specific category, such as 'Furniture'. Finally, you'll find products where the `Price` is greater than a certain value. Remember to pay close attention to whether you're comparing strings or numbers..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: Alright, it's time for some hands-on practice! We're going to work with a simulated `Products` table, which has columns like `ProductID`, `ProductName`, `Category`, `Price`, and `StockQuantity`. For this first part of the activity, your goal is to practice retrieving data using `SELECT` and `FROM`, and then filtering it with the `WHERE` clause using basic comparison operators. You'll start by selecting specific columns like `ProductName` and `Price` for all products. Then, you'll retrieve all columns for products belonging to a specific category, such as 'Furniture'. Finally, you'll find products where the `Price` is greater than a certain value. Remember to pay close attention to whether you're comparing strings or numbers..",
      "narration_script": "Alright, it's time for some hands-on practice! We're going to work with a simulated `Products` table, which has columns like `ProductID`, `ProductName`, `Category`, `Price`, and `StockQuantity`. For this first part of the activity, your goal is to practice retrieving data using `SELECT` and `FROM`, and then filtering it with the `WHERE` clause using basic comparison operators. You'll start by selecting specific columns like `ProductName` and `Price` for all products. Then, you'll retrieve all columns for products belonging to a specific category, such as 'Furniture'. Finally, you'll find products where the `Price` is greater than a certain value. Remember to pay close attention to whether you're comparing strings or numbers.",
      "estimated_duration_seconds": 50,
      "speaking_style": "default",
      "delivery_notes": [
        "Introduce the Products table.",
        "Clearly state the first set of tasks.",
        "Remind about string/numeric comparison."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Alright, it's time for some hands-on practice!",
          "estimated_duration_seconds": 3
        },
        {
          "index": 1,
          "text": "We're going to work with a simulated `Products` table, which has columns like `ProductID`, `ProductName`, `Category`, `Price`, and `StockQuantity`.",
          "estimated_duration_seconds": 9
        },
        {
          "index": 2,
          "text": "For this first part of the activity, your goal is to practice retrieving data using `SELECT` and `FROM`, and then filtering it with the `WHERE` clause using basic comparison operators.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 3,
          "text": "You'll start by selecting specific columns like `ProductName` and `Price` for all products.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 4,
          "text": "Then, you'll retrieve all columns for products belonging to a specific category, such as 'Furniture'.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 5,
          "text": "Finally, you'll find products where the `Price` is greater than a certain value.",
          "estimated_duration_seconds": 6
        },
        {
          "index": 6,
          "text": "Remember to pay close attention to whether you're comparing strings or numbers.",
          "estimated_duration_seconds": 5
        }
      ]
    },
    {
      "screen": 8,
      "type": "hands-on",
      "title": "Hands-On: Querying Product Data - Part 2",
      "content": "Continuing with the `Products` table, this second part of the activity challenges you to use logical operators (`AND`, `OR`) and parentheses within your `WHERE` clause. You'll need to combine multiple conditions to find products that meet more complex criteria, such as 'Electronics' products with low stock, or products that fall into one of two complex categories. Remember the operator precedence and use parentheses to ensure your logic is correctly applied.",
      "image_prompt": "Create a high-resolution 16:9 teaching image for Cohortia that feels like a premium interactive lesson screen, not random illustration art.\n\n**Course:** CS50's Introduction to Computer Science\n**Chapter:** SQL Fundamentals: SELECT, FROM, WHERE\n**Screen Type:** hands-on\n\n**Learning Objectives:**\n*   Construct basic SQL `SELECT` statements to retrieve data from a single table.\n*   Utilize the `FROM` clause to specify the target table for data retrieval.\n*   Apply the `WHERE` clause with various comparison and logical operators to filter rows based on specific conditions.\n*   Distinguish between selecting all columns (`SELECT *`) and selecting specific columns.\n*   Understand and use common SQL comparison operators (`=`, `!=`, `>`, `<`, `>=`, `<=`) and logical operators (`AND`, `OR`, `NOT`).\n\n**Lesson Content:**\nContinuing with the `Products` table, this second part of the activity challenges you to use logical operators (`AND`, `OR`) and parentheses within your `WHERE` clause. You'll need to combine multiple conditions to find products that meet more complex criteria, such as 'Electronics' products with low stock, or products that fall into one of two complex categories. Remember the operator precedence and use parentheses to ensure your logic is correctly applied.\n\n**Key Concepts:**\n*   **SQL (Structured Query Language):** A declarative language used to manage and manipulate data in relational database management systems.\n*   **`SELECT` clause:** Specifies the columns to be retrieved from a table.\n*   **`FROM` clause:** Indicates the table(s) from which data will be retrieved.\n*   **`*` (Asterisk):** A wildcard character used in the `SELECT` clause to retrieve all columns from the specified table.\n*   **`WHERE` clause:** Filters rows based on specified conditions, returning only the rows that satisfy those conditions.\n*   **Comparison Operators:** Symbols used in the `WHERE` clause to compare values (e.g., `=`, `!=`, `>`, `<`, `>=`, `<=`).\n*   **Logical Operators:** Keywords used to combine or modify multiple conditions in the `WHERE` clause (e.g., `AND`, `OR`, `NOT`).\n\n## Creative Direction\n\nThe image must look like a cinematic, polished learning interface with strong instructional value.\n\n- Use a dark navy or deep indigo background with neon cyan, teal, violet, and soft magenta highlights.\n- Build the scene like a modern dashboard or holographic teaching panel.\n- Favor layered depth, glassmorphism cards, glowing connectors, floating labels, subtle grids, and clear visual hierarchy.\n- Make the subject feel technical, immersive, and course-specific.\n- Keep the overall composition clean and premium.\n- Avoid generic stock-photo scenes, random fantasy landscapes, or abstract blobs that do not teach.\n\n## Cohortia Visual Style\n\nThe best Cohortia images feel like:\n\n- a futuristic learning console\n- a guided system map\n- a network or workflow visualization\n- an intelligent classroom interface\n- a cinematic but practical study aid\n\nUse small amounts of readable text only when it improves the teaching moment.\n\n## Screen Type Guidance\n\n### course-overview\n- Show a single hero composition that summarizes the whole chapter or course.\n- Include a strong central visual metaphor, the chapter title, and a path-map or architecture view.\n- Show the learning journey as a polished visual roadmap.\n\n### intro\n- Show a hero teaching moment for the learning objectives.\n- Include the title, objective cards, and one dominant concept visual.\n- Make it feel like the learner is opening an interactive lesson dashboard.\n\n### concept\n- Focus on one idea at a time.\n- Use a visual process, layered system, or cause-and-effect diagram.\n- Make the explanation feel guided and precise.\n\n### diagram\n- Show a relationship map, architecture flow, or system topology.\n- Connections, nodes, labels, and hierarchy must be clear.\n- Prefer elegant technical diagrams with depth and glow.\n\n### hands-on\n- Show one practical scenario, lab, or troubleshooting moment.\n- Make the scene feel like a guided workshop with interface overlays or step cards.\n\n### code-heavy\n- If the lesson includes code, terminal output, config, or commands, render the screen as a polished VS Code-style teaching workspace.\n- Show the code clearly, with syntax highlighting, file tabs, a file tree, terminal panel, and teachable callouts.\n- Make the code the hero of the composition, not a tiny background detail.\n- Keep the scene course-specific and tied directly to the narrator's explanation, not a generic coding mockup.\n\n## Composition Rules\n\n- Resolution: 1920x1080\n- Perspective: cinematic, layered, readable\n- Lighting: soft glow, rim light, controlled highlights\n- Typography: minimal, modern, and clean\n- Mood: confident, instructional, futuristic, premium\n- Visual priority: the teaching idea must be obvious at a glance\n\n## What To Avoid\n\n- No clutter\n- No cluttered paragraphs of text\n- No random decorative graphics that do not support the lesson\n- No vague abstract art\n- No cheap sci-fi overload\n- No people unless the lesson truly benefits from a teacher, learner, or team presence\n\nGenerate a single immersive teaching image that looks like a real Cohortia lesson screen.\n\n\nThe image must directly represent the syllabus being taught, not a generic related concept. Show the exact teaching idea from CS50's Introduction to Computer Science and 5.2: SQL Fundamentals: SELECT, FROM, WHERE. Visualize the narrator's explanation with a clear instructional layout, matching the pacing and emphasis of the narration. Prefer a clean, immersive Cohortia lesson interface with course-relevant labels, diagrams, and visual hierarchy. Source content: Continuing with the `Products` table, this second part of the activity challenges you to use logical operators (`AND`, `OR`) and parentheses within your `WHERE` clause. You'll need to combine multiple conditions to find products that meet more complex criteria, such as 'Electronics' products with low stock, or products that fall into one of two complex categories. Remember the operator precedence and use parentheses to ensure your logic is correctly applied.. Narration focus: Now, let's take your filtering skills up a notch with the second part of our hands-on activity. Using the same `Products` table, you'll tackle more complex scenarios by combining conditions with `AND` and `OR` operators, and critically, by using parentheses to control the order of evaluation. First, you'll find 'Electronics' products that also have a `StockQuantity` less than 100, requiring an `AND` condition. Then, for the ultimate challenge, you'll construct a query to find products that are either 'Electronics' and priced under 100, OR are 'Furniture' products. This will test your understanding of grouping conditions with parentheses. Remember, precision is key when building these complex `WHERE` clauses..\nThe screen must be highly detailed and faithful to the syllabus and to the narrator's explanation.\nDo not misinterpret or abstract the topic into unrelated symbolism.\nEvery visual element must clearly represent the exact concept, term, or process being taught.\nThe layout, labels, diagrams, and focal points must follow the teaching sequence so the learner sees what the voice is explaining at that moment.\nKeep the teaching UI premium, cinematic, and technically accurate.\nThis screen is code-heavy. Render it as a polished VS Code teaching workspace. Use an editor window with a file tree, tabs, syntax-highlighted code, a terminal panel, and instructor callout annotations. Make the code itself the hero of the scene, with clear highlighting of the exact lines being explained. Add subtle teaching overlays such as numbered steps, arrows, inline notes, and a code review side panel. The scene should feel like a real computer science lesson in progress, not a generic screenshot. Match the course and chapter precisely: CS50's Introduction to Computer Science | SQL Fundamentals: SELECT, FROM, WHERE. The visual should reflect the narration tone and explanation flow: Now, let's take your filtering skills up a notch with the second part of our hands-on activity. Using the same `Products` table, you'll tackle more complex scenarios by combining conditions with `AND` and `OR` operators, and critically, by using parentheses to control the order of evaluation. First, you'll find 'Electronics' products that also have a `StockQuantity` less than 100, requiring an `AND` condition. Then, for the ultimate challenge, you'll construct a query to find products that are either 'Electronics' and priced under 100, OR are 'Furniture' products. This will test your understanding of grouping conditions with parentheses. Remember, precision is key when building these complex `WHERE` clauses..",
      "narration_script": "Now, let's take your filtering skills up a notch with the second part of our hands-on activity. Using the same `Products` table, you'll tackle more complex scenarios by combining conditions with `AND` and `OR` operators, and critically, by using parentheses to control the order of evaluation. First, you'll find 'Electronics' products that also have a `StockQuantity` less than 100, requiring an `AND` condition. Then, for the ultimate challenge, you'll construct a query to find products that are either 'Electronics' and priced under 100, OR are 'Furniture' products. This will test your understanding of grouping conditions with parentheses. Remember, precision is key when building these complex `WHERE` clauses.",
      "estimated_duration_seconds": 55,
      "speaking_style": "default",
      "delivery_notes": [
        "Introduce more complex tasks.",
        "Emphasize `AND`, `OR`, and parentheses.",
        "Highlight the challenge of the last query."
      ],
      "segment_breaks": [
        {
          "index": 1,
          "text": "Now, let's take your filtering skills up a notch with the second part of our hands-on activity.",
          "estimated_duration_seconds": 7
        },
        {
          "index": 1,
          "text": "Using the same `Products` table, you'll tackle more complex scenarios by combining conditions with `AND` and `OR` operators, and critically, by using parentheses to control the order of evaluation.",
          "estimated_duration_seconds": 12
        },
        {
          "index": 2,
          "text": "First, you'll find 'Electronics' products that also have a `StockQuantity` less than 100, requiring an `AND` condition.",
          "estimated_duration_seconds": 10
        },
        {
          "index": 3,
          "text": "Then, for the ultimate challenge, you'll construct a query to find products that are either 'Electronics' and priced under 100, OR are 'Furniture' products.",
          "estimated_duration_seconds": 11
        },
        {
          "index": 4,
          "text": "This will test your understanding of grouping conditions with parentheses.",
          "estimated_duration_seconds": 5
        },
        {
          "index": 5,
          "text": "Remember, precision is key when building these complex `WHERE` clauses.",
          "estimated_duration_seconds": 5
        }
      ]
    }
  ]
}