# Backend API base URL. Use /api/v1 locally; production falls back to the Render API.
VITE_API_URL=/api/v1
# OAuth endpoint that starts the Google sign-in flow.
VITE_GOOGLE_AUTH_URL=/api/v1/auth/google
